继续 top10-decision 主线开发，严格以《核心算法需求规范（Core Algorithm Spec).MD》为最高锚定，不跑偏。

当前系统状态已经确认：

1. 当前主线是 Decision 五模块决策引擎：
   - P_fill / Execution Fill Probability Module（可成交概率模块）
   - E_ret / Expected Return Module（预期收益模块）
   - Cost / Transaction Cost Module（交易成本模块）
   - RiskPenalty / Risk Penalty Module（风险惩罚模块）
   - EV / Expected Value Module（综合价值模块）

2. 当前开发阶段只完成了第一个模块：
   - P_fill 学习链首轮闭环已经打通
   - 已跑通 20260302
   - 关键产物已生成：
     - fill_truth_20260302.csv
     - pfill_trainset_20260302.csv
     - pfill_lr / pfill_lgbm / pfill_meta

3. 已经确认并修正的关键逻辑：
   - 后续所有计算对象必须以 pred_source 候选池名单为主
   - FS / raw 只能给候选池股票补字段，不能反向扩池
   - build_fill_truth.py 已改成 pred_source 驱动
   - build_pfill_trainset.py 主表是 fill_truth，不再扩池

4. 已确认的训练时间机制（已写入 Core Algorithm Spec）：
   - T 日先预测未来
   - T 日只训练今天刚成熟的过去样本
   - P_fill / PredOpen_T+1：用 T 日真值训练 T-1 对 T 的预测
   - E_ret / PredClose_T+2 / PremiumRet：用 T 日真值训练 T-2 对 T 的预测
   - T 日训练出的新模型，只能从 T+1 生效
   - 当天若训练条件不足，允许 skip，但不得打死主预测链

5. 当前已暴露的问题：
   - 20260303~20260306 中某些日期会报：
     - 单一标签（labels=[1]）
     - 或缺少 T+1 raw，无法自动推断 exec_date
   - 这说明训练日期机制还没独立成正式基础设施

6. 当前下一步主线已经锁死：
   - 不是继续补丁某一天
   - 而是正式建立训练日期机制
   - 优先新增：
     scripts/resolve_sample_maturity.py
   - 职责：
     - 给定当前运行日 / raw 快照 / 候选 trade_date
     - 输出每个 trade_date 的：
       - trade_date
       - exec_date
       - target_date
       - sample_maturity
       - 是否 PFILL_READY
       - 是否 ERET_READY
       - 是否 FULLY_READY

7. 后续改动顺序锁死：
   1) 新增 scripts/resolve_sample_maturity.py
   2) 改 run_decision_daily.yml，让它先调用成熟度解析器
   3) 改 build_fill_truth.py，去掉自己临时猜 exec_date 的职责
   4) 再升级 train_pfill.py，从单日训练升级为成熟窗口训练

8. 协作规则保持：
   - 一个文件一个文件来
   - 每次先给：
     1）文件全路径
     2）简要修改报告
     3）完整可覆盖代码
   - 超过 300 行文件，优先基于 Raw 链接修改
   - 全部使用简体中文
- 明天不是继续补丁某一天，而是正式做 scripts/resolve_sample_maturity.py
