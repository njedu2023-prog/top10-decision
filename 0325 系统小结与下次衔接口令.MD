# top10-decision｜0325 系统小结与下次衔接口令

## 一、站在全系统视角看：这轮到底是在把什么升级到什么

这轮不是在做表面层的小修小补，也不是单纯把某个 workflow 跑绿。

这轮真正做的升级，是把 **Top10 Decision** 从：

**“依赖局部脚本、局部文件、局部日期偶然跑通的半闭环系统”**

升级到：

**“围绕公开真值代理语义，可以稳定落盘、稳定训练、可逐日推进的主链闭环系统”**

更具体地说，升级的不是单一脚本，而是整个系统的学习底座口径。

以前系统更接近：

- 候选池能出来
- 排序能出来
- workflow 有时能跑
- 但学习链是否真的闭合、truth 是否真落盘、trainset 是否持续可训练、model/meta 是否真按成熟日推进，并不稳定

现在系统正在升级为：

- **P_fill** 有明确代理成交真值
- **EntryPriceProxy_T+1** 有明确代理买入价语义
- **E_ret** 有明确代理收益真值
- **sample_maturity** 有明确成熟推进规则
- **truth → trainset → model/meta** 四层可以按成熟日真实落盘
- 学习链开始从“偶然跑通一次”进入“可以持续推进”的阶段

### 这对系统最高价值的好处是什么

系统最高目标不是“生成一个看起来很聪明的 TopN 列表”，而是：

在 T 日收盘后，输出一个 **T+1 能买进去、T+1→T+2 有正期望、风险可控、还能持续自学习优化** 的可执行组合。

这轮升级对这个最高目标的价值非常大，原因是它在补系统最关键的地基：

#### 1. 让系统开始真正学习“可买性”，而不是只会排序

A-top10 上游擅长找强票，但强票不等于可成交。  
Top10 Decision 的核心价值，就是把“强”进一步筛成“明天能买且买后仍有价值”。

这轮升级，把 **P_fill** 从模糊判断，推进成了基于公开行情代理规则的真实学习对象。

这意味着系统以后不是只会说“这票强”，而是更接近回答：

- 明天能不能买到？
- 以什么代理成交语义定义“买到”？
- 哪类票虽然强，但几乎不可成交？
- 哪类票虽不最强，但成交概率和后续收益组合更优？

#### 2. 让系统开始真正学习“买后收益”，而不是停留在静态打分

如果没有 `EntryPriceProxy_T+1`，那 E_ret 就没有统一锚点。  
没有统一买入价定义，收益标签就漂。

这轮升级锁定了：

- EntryPriceProxy_T+1
- close_T2
- E_ret 的代理收益口径

这一步的价值在于：  
系统开始从“泛化的强弱打分器”，升级为“围绕可执行买点定义收益期望的学习系统”。

#### 3. 让系统从“单日跑通”升级到“逐日推进”

只跑通一次没有系统价值。  
真正有价值的是：

- 今天能跑
- 明天还能跑
- 成熟样本会自然推进
- 模型会自然迭代
- 新 truth / 新 trainset / 新 meta 会持续落盘

这次你看到的关键进步，不是某个文件有了，而是：

- `pfill_meta.json` 已推进到 `20260324`
- `pfill_trainset_20260324.csv` 已真实形成
- `fill_truth_20260324.csv` 已真实存在
- `eret_meta.json` 也已经 trained，只是当前锚点还在 `20260323`

这说明系统已经开始接近“持续推进型学习链”，而不再只是临时拼接。

#### 4. 让后续优化有了坚实抓手

只有当 truth / trainset / model/meta 四层都稳定落盘后，后面这些高级优化才有真实意义：

- audit / meta 增强
- degrade 行为统计
- entry_price_proxy_mode 分布分析
- 缺失真值比例跟踪
- 风险代理更精细化
- EV 聚合更稳
- 模型回溯和解释增强

否则前面基础没闭合，后面再聪明的优化都只是漂在空中。

---

## 二、当前成果小结：比之前实质进步了什么

### 1. 系统阶段已经变了

之前系统处在：

**“公开真值代理执行语义主链是否真的打通”**

现在系统已经进入：

**“主链收口完成后的稳定化验收与基线固化阶段”**

这是阶段性跃迁，不是简单修 bug。

### 2. P_fill 主链已经不再是假闭环

当前已经确认：

- `data/market/fill_truth_20260324.csv` 已存在
- `data/market/pfill_trainset_20260324.csv` 已形成
- `models/pfill_meta.json` 已 trained
- `anchor_trade_date = 20260324`

这说明 P_fill 已经完成了：

**truth 层 → trainset 层 → model/meta 层**

的真实落盘闭环。

这比之前“workflow 似乎跑了，但不知道有没有真训练成功”的状态，强很多。

### 3. E_ret 主链也已经不是空转

当前已经确认：

- `models/eret_meta.json` 为 `trained`
- `anchor_trade_date = 20260323`
- 已输出 dated 模型与 latest 模型
- train / valid 都不是空壳

这说明 E_ret 不是未接通，而是已经进入真实训练状态。  
只是当前最新锚点还停在 `20260323`，还没有像 P_fill 那样推进到 `20260324`。

这和以前“看起来有 E_ret 逻辑，但不确定是否真实落盘”的状态相比，也是实质进步。

### 4. workflow 的断点已经从“主链断”变成“窗口缺口”

现在暴露出来的问题，不再是：

- 口径没锁死
- 字段名错配
- 根本不训练
- truth 不落盘
- model 不更新

而是更高一级的问题：

- 历史成熟窗口里还有个别 trade_date 缺口
- `sample_maturity` 的持续推进需要继续固化
- 某些成熟日虽然进入窗口，但对应 trainset 未完全补齐

这说明系统问题层级已经上升了。  
问题从“主链根本没通”升级成了“已通，但历史窗口还不够完整”。

### 5. 现在比之前更接近系统最高价值

以前系统更像：

- 一个很强的候选池消费器
- 一个带有一定规则增强的执行筛选器
- 一个开始尝试学习的半自动系统

现在系统正在更接近：

- 一个有明确代理成交语义的可买性学习系统
- 一个有统一买入价语义的收益学习系统
- 一个可以随成熟样本日持续推进的闭环系统
- 一个未来有能力持续提高 EV 决策质量的系统

换句话说：

**之前更像“能工作”；现在开始更像“能进化”。**

---

## 三、当前仍未完全收口的地方

虽然进步很大，但还不能误判成“全部收工”。

当前还没完全收口的重点有两个：

### 1. 历史窗口仍有缺口

从当前 meta 看：

- P_fill 缺口仍包括部分历史成熟日
- E_ret 也还有部分历史成熟日未完整进入窗口

这不影响当前主链已经能往前推进，  
但会影响训练窗口完整性与长期稳健性。

### 2. 验收基线还没彻底固化

现在已经能确认若干关键对象真实存在，但后续还需要把这套验收对象正式固定为标准基线：

- `sample_maturity_latest.csv`
- `fill_truth_{trade_date}.csv`
- `pfill_trainset_{trade_date}.csv`
- `eret_truth_{trade_date}.csv`
- `eret_trainset_{trade_date}.csv`
- `models/pfill_meta.json`
- `models/eret_meta.json`

只有基线固定后，后续每次推进才不会又回到“大范围猜哪里坏了”的低效状态。

---

## 四、下次继续时的无缝衔接口令

下次进入本仓库主线后，直接按下面口令继续：

---

继续 top10-decision 主线。

当前阶段不是重做 workflow，不是改上下游路径，不是推倒重来，而是已经完成“公开真值代理执行语义”的主链收口，当前主线已进入“稳定化验收 + 历史窗口补齐 + 基线固化”阶段。

当前系统状态锚点如下：

1. P_fill 主链已推进到 `20260324`
- `data/market/fill_truth_20260324.csv` 已存在
- `data/market/pfill_trainset_20260324.csv` 已存在且非空
- `models/pfill_meta.json` 已 trained
- `anchor_trade_date = 20260324`

2. E_ret 主链已真实 trained，但当前锚点仍为 `20260323`
- `models/eret_meta.json` 已 trained
- 当前仍需继续验收 `eret_truth / eret_trainset / 历史窗口缺口`

3. 当前主问题已经不是“主链是否能打通”
而是：
- 历史成熟窗口是否完整
- `sample_maturity_latest.csv` 推进是否持续稳定
- 验收基线是否正式固化
- 是否仍存在未来某天再次断链的风险

下次进入后，直接按以下顺序执行：

### 第一步：继续做最小验收，不发散新功能

优先检查：

- `data/market/eret_truth_20260323.csv`
- `data/market/eret_trainset_20260323.csv`
- `data/market/sample_maturity_latest.csv`

目标：

确认 E_ret truth / trainset / model-meta 是否与当前 maturity 状态一致，  
确认 `PFILL_READY / ERET_READY / FULLY_READY` 口径是否与真实落盘文件一致。

### 第二步：处理历史窗口缺口

目标：

不要重建主链，只对当前 meta 暴露出的 `missing_trade_dates` 做最小安全补齐。

重点：

- 先定位缺的是 `pred_source`、`fill_truth`、`eret_truth` 还是 trainset
- 只做最小补齐
- 不改外部契约
- 不改路径
- 不破坏现有 workflow 主链

### 第三步：固化标准验收基线

固定后续标准验收对象：

- `sample_maturity_latest.csv`
- `fill_truth_{trade_date}.csv`
- `pfill_trainset_{trade_date}.csv`
- `eret_truth_{trade_date}.csv`
- `eret_trainset_{trade_date}.csv`
- `models/pfill_meta.json`
- `models/eret_meta.json`

以后优先围绕这套对象验收，不再重复大范围排查。

### 第四步：主链稳定后，再考虑次级增强

这些不是当前第一优先：

- audit / meta 增强
- degrade 行为统计
- 文档旧字段/旧枚举收口
- Node.js 24 兼容升级

---

## 五、下次输出要求（继续锁死）

每次推进后，固定按以下四段输出：

### 1）改了哪个文件
明确到文件路径；若没改文件，就写“本轮未改文件”。

### 2）为什么改
必须直接指向主线问题，不讲空话。

### 3）完整可覆盖版
如涉及代码或 workflow，优先给完整可覆盖版。

### 4）下一步主线是什么
必须只给主线，不要发散。

---

## 六、当前一句话总结

当前 top10-decision 已经从“公开真值代理执行语义是否打通”的不确定阶段，升级到了“P_fill 已真实闭环、E_ret 已真实训练、下一步进入历史窗口补齐与验收基线固化”的阶段。

这比之前的最大进步不是多了几个文件，而是：

**系统开始从‘能工作’走向‘能持续学习和持续进化’。**
