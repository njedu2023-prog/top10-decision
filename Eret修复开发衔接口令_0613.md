# E_ret 修复开发衔接口令｜top10-decision Decision 主线

更新时间：2026-06-13  
主线：top10-decision / Decision / E_ret 修复 / Feature Store 修复工程  
当前状态：`scripts/build_market_fs.py` 已覆盖为 Feature Store V2 版本，下一步进入验收与 E_ret 重训。

---

## 一、下次进入后的固定口令

继续 top10-decision Decision 主线，不切 Premium，不回退旧话题，不重做 workflow，不改前端，不改上下游路径。

当前任务是：验收 E_ret 修复链路，重点确认 `scripts/build_market_fs.py` 的 Feature Store V2 修复是否真正生效。

这次修复的核心目标：

1. 修复 `pct_chg` 单位问题；
2. 修复 `pre_close_est` 反推错误；
3. 接入历史 daily.csv；
4. 让以下字段不再长期全空：

```text
ret_2d
ret_5d
ret_10d
volatility_5d
volatility_10d
volatility_20d
atr
downside_vol
max_drawdown_20d
tail_risk_score
bid_ask_proxy
spread_proxy
```

---

## 二、当前已完成的修改

已修改并覆盖文件：

```text
scripts/build_market_fs.py
```

核心变化：

### 1. 新增历史日线读取

新增历史 raw daily 自动扫描逻辑，读取：

```text
data/market/raw/{YYYY}/{YYYYMMDD}/daily.csv
```

并兼容旧结构：

```text
data/market/raw/daily_{YYYYMMDD}.csv
```

### 2. 修复 pct_chg 单位

原问题：

```text
pct_chg=10.0 被当成 +1000%
```

导致：

```text
pre_close_est = close / (1 + 10)
```

严重错误。

修复后：

```text
pct_chg=10.0 按 +10% 处理
pre_close_est = close / (1 + pct_chg / 100)
```

### 3. 新增历史滚动特征

新增并接入：

```text
ret_2d
ret_5d
ret_10d
volatility_5d
volatility_10d
volatility_20d
atr
downside_vol
max_drawdown_20d
tail_risk_score
bid_ask_proxy
spread_proxy
```

### 4. 更新 FS 版本

`build_meta()` 中：

```text
fs_version = v0.5-history-roll-features
```

---

## 三、为什么这次修复很关键

之前 `models/eret_meta.json` 显示：

```text
ret_2d = 1.0 missing
ret_5d = 1.0 missing
ret_10d = 1.0 missing
volatility_5d = 1.0 missing
volatility_10d = 1.0 missing
volatility_20d = 1.0 missing
atr = 1.0 missing
downside_vol = 1.0 missing
max_drawdown_20d = 1.0 missing
tail_risk_score = 1.0 missing
```

这说明 E_ret 模型长期缺少有效的收益、趋势、波动、风险特征。

模型验证结果也明显异常：

```text
corr 接近 0 且为负
r2 为负
directional_acc 接近随机
```

所以 EV 长期为负，根因不是前端，也不是单纯 EV 公式，而是 E_ret 的特征层长期残缺。

---

## 四、下一步验收顺序

### 第一步：确认 Actions 已跑通

看 GitHub Actions：

```text
Run Top10 Decision (Auto Daily)
```

当前页面已经看到：

```text
Run Top10 Decision (Auto Daily) #219 completed successfully
```

这说明 workflow 层面没有直接炸掉。

但这还不是最终验收，只是说明脚本链路大概率跑过了。

---

### 第二步：验收 Feature Store 输出

打开：

```text
data/market/features_base_20260609.csv
```

重点检查这些字段：

```text
ret_2d
ret_5d
ret_10d
volatility_5d
volatility_10d
volatility_20d
atr
downside_vol
max_drawdown_20d
tail_risk_score
bid_ask_proxy
spread_proxy
```

验收标准：

```text
这些字段不能再全部为空。
```

如果这些字段仍然全空，说明历史 raw daily 没有被正确读取，或者 `build_features_base()` 没有成功 merge 历史特征。

---

### 第三步：验收 meta 文件

打开：

```text
data/market/_meta_20260609.json
```

重点看：

```text
fs_version
features_base.non_null_rate_by_col
```

应看到：

```text
fs_version = v0.5-history-roll-features
```

并且以下字段 non_null_rate 应大于 0：

```text
ret_2d
ret_5d
ret_10d
volatility_5d
volatility_10d
volatility_20d
atr
downside_vol
max_drawdown_20d
tail_risk_score
```

---

### 第四步：验收 E_ret 训练集

打开：

```text
data/market/eret_trainset_20260609.csv
```

重点检查：

```text
ret_2d
ret_5d
ret_10d
volatility_5d
volatility_10d
volatility_20d
atr
downside_vol
max_drawdown_20d
tail_risk_score
```

验收标准：

```text
这些字段在 eret_trainset 中也不能全空。
```

如果 features_base 有值，但 eret_trainset 还是空，问题在：

```text
scripts/build_eret_trainset.py
```

如果 features_base 本身就空，问题仍在：

```text
scripts/build_market_fs.py
```

---

### 第五步：验收 E_ret 训练结果

打开：

```text
models/eret_meta.json
```

重点看：

```text
missing_ratio
metrics_valid
```

要对比旧状态：

```text
ret_2d = 1.0
ret_5d = 1.0
ret_10d = 1.0
volatility_5d = 1.0
volatility_10d = 1.0
volatility_20d = 1.0
atr = 1.0
downside_vol = 1.0
max_drawdown_20d = 1.0
tail_risk_score = 1.0
```

修复后，理想状态：

```text
这些字段 missing_ratio 明显小于 1.0
```

至少应满足：

```text
ret_2d / ret_5d / ret_10d / volatility_5d / volatility_10d / volatility_20d 不再是 1.0
```

然后看模型指标：

```text
corr 是否从负数接近 0 改善为正
r2 是否不再明显恶化
directional_acc 是否高于随机
```

注意：一次修复不一定马上让模型非常强，但至少不能再是“核心特征 100% 缺失”。

---

## 五、如果验收失败，按这个分支排查

### 情况 A：workflow 失败

先打开失败日志，看报错文件。

优先怀疑：

```text
scripts/build_market_fs.py
```

常见问题：

```text
pandas rolling apply 报错
列名不存在
历史目录读取失败
类型转换失败
```

---

### 情况 B：workflow 成功，但 features_base 字段仍全空

优先检查：

```text
data/market/raw/2026/
```

是否保留多个历史交易日目录。

至少需要类似：

```text
20260609
20260608
20260605
20260604
20260603
...
```

如果只有当天目录，则 `ret_10d` / `volatility_20d` 不可能计算。

---

### 情况 C：features_base 有值，但 eret_trainset 为空

优先检查：

```text
scripts/build_eret_trainset.py
```

重点看 merge 逻辑是否保留了 Feature Store 新字段。

---

### 情况 D：eret_meta missing_ratio 降了，但模型指标还差

这是正常的第二阶段问题。

下一步再修：

```text
scripts/train_eret.py
```

可能要加入：

```text
全空特征自动剔除
高缺失特征审计
时间切分稳定性检查
特征重要性输出
LR/LGBM 对比择优
```

但不要现在就改。先完成 Feature Store 第一阶段验收。

---

## 六、当前不要做的事情

不要先改：

```text
scripts/run_v2.py
src/top10decision/engines/eret_engine.py
scripts/train_eret.py
scripts/build_eret_trainset.py
decision.html
```

当前优先级：

```text
先验收 Feature Store 是否成功。
```

如果 Feature Store 没有成功，后面所有训练和 EV 都是假的。

---

## 七、最终验收标准

本轮 E_ret 修复第一阶段验收通过，需要满足：

```text
1. Run Top10 Decision workflow 成功；
2. features_base_20260609.csv 中历史收益/波动字段不再全空；
3. _meta_20260609.json 中 fs_version = v0.5-history-roll-features；
4. eret_trainset_20260609.csv 中对应字段不再全空；
5. models/eret_meta.json 中相关字段 missing_ratio 不再等于 1.0；
6. Decision 输出中 E_ret 不再整体塌缩为接近 0；
7. EV 不再大量机械性全负。
```

---

## 八、下一次进入后的第一句话

直接说：

```text
继续 top10-decision Decision 主线，验收 E_ret / Feature Store V2 修复。
先看 features_base_20260609.csv 和 _meta_20260609.json，不要切 Premium，不要改前端。
```
