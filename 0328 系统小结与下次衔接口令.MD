# 0328 的衔接口令

继续 top10-decision 主线，不回退旧话题，不重做 workflow，不改上下游路径，不推倒重来。

## 一、当前主线阶段定位

当前阶段已经不是“主链能不能打通”，而是：

**稳定化验收 + 历史窗口补齐 + 基线固化**

本阶段的系统目标不是发散新功能，而是把已经打通的公开真值代理执行语义主链，进一步收口成：

- 可稳定执行
- 可持续推进
- 能真实回补历史窗口缺口
- 验收基线清晰固定
- 后续不再反复大范围排查

---

## 二、截至 2026-03-28 的系统状态锚点

### 1. workflow 层状态
`run_decision_daily.yml` 已从“反复报错 / 不能执行”恢复到**可成功运行**状态。

最近一次手动运行已经成功，说明：
- `decision` job 可跑
- `pfill_learning` job 可跑
- 当前主问题已不再是 workflow 语法/缩进报错

注意：
- 当前 workflow 虽然可跑，但它的 learning 选日逻辑是否已完全对齐系统目标，仍需继续验收，不可轻率再大改整个 workflow。

---

### 2. P_fill 当前状态
当前 `models/pfill_meta.json` 的核心状态：

- `anchor_trade_date = 20260326`
- `status = trained`
- 历史窗口已推进到较后阶段
- 但 `missing_trade_dates` 仍包含：
  - `20260227`
  - `20260323`

已确认：
- `data/market/fill_truth_20260323.csv` 已存在，且样本口径正常
- 但 `data/market/pfill_trainset_20260323.csv` 仍不存在

这说明：
- `20260323` 这一天不是 maturity 没到
- 不是 fill_truth 没有
- 而是 **P_fill 的 trainset 层仍未完全补齐**

**当前 P_fill 最关键未收口缺口：`20260323`**

---

### 3. E_ret 当前状态
当前 `models/eret_meta.json` 的核心状态：

- `anchor_trade_date = 20260320`
- `status = trained`
- 当前 `missing_trade_dates` 只剩：
  - `20260227`

已确认：
- `20260320` 已经被补进 E_ret 训练窗口
- `loaded_trade_dates` 已覆盖到 `20260320`
- `loaded_trainset_paths` 已包含 `eret_trainset_20260320.csv`

这说明：
- **E_ret 的 `20260320` 缺口，已实质补齐**
- 当前 E_ret 主线相比之前已有明显收口进展

**当前 E_ret 主要只剩更老的 `20260227` 缺口**

---

## 三、站在系统视角的阶段性小结

这轮最大的系统性进步不是“多了几个文件”，而是：

### 1. 系统已经从“workflow 层不稳定”推进到“主链可运行”
之前的问题层级是：
- workflow 语法报错
- learning job 缩进报错
- 连执行都不稳定

现在已经推进到：
- workflow 可成功运行
- learning 能启动并执行
- 问题层级上升为“历史窗口缺口补齐是否到位”

这是一个明确的阶段跃迁。

### 2. E_ret 已证明“历史缺口是可以被补进训练窗口的”
之前 E_ret 的 `20260320` 是缺口。
现在它已进入 `anchor_trade_date = 20260320` 的真实 trained 状态。

这说明：
- 缺口补齐链路不是空想
- 当前系统已经具备“历史回补”的真实能力
- 后面重点是把 P_fill 侧也完全收口

### 3. 当前系统最大短板已收缩为更小范围
不再是全链路混乱，而是收缩为：
- P_fill 的 `20260323` 单点缺口
- P_fill / E_ret 都还共有 `20260227` 老缺口

也就是说，系统问题已经从“大范围不确定”收缩成了“少数历史窗口缺口”。

---

## 四、当前不要做的事

下次继续时，**不要发散到这些方向**：

- 不要重写整个 workflow
- 不要重做上下游路径
- 不要推倒已有 learning 主链
- 不要优先去做 Node.js 24 兼容升级
- 不要优先做 audit / meta 增强
- 不要优先做 degrade 统计
- 不要优先做旧字段 / 旧枚举文档收口

这些都不是当前第一优先。

---

## 五、下次继续时的唯一主线

### 第一优先：继续打穿 P_fill 的 `20260323` 缺口
这是当前最值得优先收口的点，因为：

- `fill_truth_20260323.csv` 已存在
- maturity 已到
- 但 `pfill_trainset_20260323.csv` 仍不存在
- `pfill_meta.json` 仍把它记为缺口

所以要优先弄清：

**为什么 `fill_truth_20260323.csv` 已有，但 `pfill_trainset_20260323.csv` 仍未落盘 / 未进入窗口。**

这比先碰更老的 `20260227` 更有价值，因为：
- 它更靠近当前训练前沿
- 一旦打穿，能证明 P_fill 的“历史缺口回补链”也成立

### 第二优先：在 P_fill 的 `20260323` 收口后，再回头处理 `20260227`
等 `20260323` 收口后，再继续推进：
- P_fill 的 `20260227`
- E_ret 的 `20260227`

只有到那一步，系统才会接近真正意义上的：
- 历史窗口基本完整
- 验收基线可正式固化

---

## 六、下次继续时，优先验收的固定对象

以后优先围绕这套对象验收，不再重复大范围排查：

- `data/market/sample_maturity_latest.csv`
- `data/market/fill_truth_{trade_date}.csv`
- `data/market/pfill_trainset_{trade_date}.csv`
- `data/market/eret_truth_{trade_date}.csv`
- `data/market/eret_trainset_{trade_date}.csv`
- `models/pfill_meta.json`
- `models/eret_meta.json`

---

## 七、下次进入后，直接执行口令

下次进入本仓库主线后，直接按下面口令继续：

---

继续 top10-decision 主线，不回退旧话题，不重做 workflow，不改上下游路径，不推倒重来。

当前阶段已经不是“workflow 能不能跑”，而是“稳定化验收 + 历史窗口补齐 + 基线固化”。

当前系统状态锚点如下：

1. workflow 已恢复到可执行状态，最近一次手动运行成功，当前主问题已不再是 workflow 语法或缩进报错。  
2. P_fill 当前 `anchor_trade_date = 20260326`，但 `missing_trade_dates` 仍包含 `20260227` 与 `20260323`。  
3. `fill_truth_20260323.csv` 已存在，但 `pfill_trainset_20260323.csv` 仍不存在。  
4. E_ret 当前 `anchor_trade_date = 20260320`，`20260320` 缺口已实质补齐，当前 `missing_trade_dates` 只剩 `20260227`。  
5. 当前系统最大短板已收缩为：优先打穿 P_fill 的 `20260323` 单点缺口，再继续处理更老的 `20260227` 缺口。  

下次进入后，直接按以下顺序执行：

### 第一步：继续最小验收，不发散新功能
优先围绕以下对象继续核对：

- `data/market/sample_maturity_latest.csv`
- `data/market/fill_truth_20260323.csv`
- `data/market/pfill_trainset_20260323.csv`
- `models/pfill_meta.json`
- `models/eret_meta.json`

目标：
- 确认 `20260323` 的 P_fill 缺口到底卡在 trainset 落盘，还是训练窗口纳入
- 确认 E_ret 当前是否已稳定收敛到只剩 `20260227`

### 第二步：只处理当前缺口，不改大结构
目标：

不要重建主链，不要大改 workflow，只对当前剩余缺口做最小安全补齐。

当前第一优先缺口：
- `P_fill / 20260323`

当前第二优先缺口：
- `P_fill / 20260227`
- `E_ret / 20260227`

### 第三步：继续固化标准验收基线
以后优先围绕这套对象验收：

- `sample_maturity_latest.csv`
- `fill_truth_{trade_date}.csv`
- `pfill_trainset_{trade_date}.csv`
- `eret_truth_{trade_date}.csv`
- `eret_trainset_{trade_date}.csv`
- `models/pfill_meta.json`
- `models/eret_meta.json`

### 第四步：在历史窗口缺口基本收口后，再考虑次级增强
这些仍不是当前第一优先：
- audit / meta 增强
- degrade 行为统计
- 文档旧字段 / 旧枚举收口
- Node.js 24 兼容升级

---

## 八、下次输出要求（继续锁死）

每次推进后，固定输出：

1. 如果有修改文件就提醒，没有修改就不要提醒  
2. 如涉及代码 / workflow，优先给完整可覆盖版  
3. 如涉及文件较长，优先给下载文件方式覆盖  
4. 明确说明“下一步主线是什么”，且只给主线，不发散

---

## 九、当前一句话总结

当前 top10-decision 已从“workflow 层报错与主链能否执行”的不稳定阶段，推进到“workflow 已恢复、E_ret 已实质补缺、下一步集中打穿 P_fill 的 `20260323` 单点缺口”的阶段。
