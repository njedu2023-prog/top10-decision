# 0331 无缝衔接口令.MD

## 一、当前主线阶段定位

继续 `top10-decision` 主线，不回退旧话题，不重做 workflow，不改上下游路径，不推倒重来。

当前阶段已经从：

- workflow 能不能跑
- learning latest 是否会被旧窗口覆盖
- P_fill / E_ret 的 latest meta 是否能稳定落盘

推进到了：

**学习主链已基本收口，当前主线切换为：审视学习系统对决策层的真实价值，并定位 E_ret / P_fill 在 decision 层里的有效性与压缩问题。**

也就是说，当前已经不是“主链打不通”的问题，而是：

**学习系统虽然能跑、能训练、能验收，但它们在决策层的输出质量、区分度、稳定性，是否真的符合系统最高价值。**

---

## 二、截至 2026-03-31 的当前状态小结

### 1. workflow / learning 主链状态

`run_decision_daily.yml` 已完成一轮关键收口：

- latest READY 选日逻辑已修正
- `Show learning artifacts (focused)` 报错已修复
- `Commit learning artifacts` 能正常执行
- `pfill_meta.json` / `eret_meta.json` 已能重新推进到前沿锚点
- `learning_acceptance_latest.md / .json` 已能产出

当前 learning 主链不再是主要矛盾。

### 2. P_fill 当前状态

当前 latest 验收显示：

- `anchor_trade_date = 20260327`
- `loaded_trade_dates = 21`
- `missing_trade_dates = 0`
- `acceptance_pass = PASS`

说明：

- P_fill 学习窗口已推进到前沿
- 历史窗口缺口已清零
- latest 工件已能稳定收口

但在 `decision_report_20260331.md` 中观察到：

- 大多数候选 `P_fill = 0.98`
- 仅极少数略低于 0.98

这说明：

**P_fill 在 decision 层当前几乎接近“封顶常数项”，区分度明显不足。**

结论：

- P_fill 工程链路已收口
- P_fill 决策层有效区分能力，尚未收口

### 3. E_ret 当前状态

当前 latest 验收显示：

- `anchor_trade_date = 20260326`
- `loaded_trade_dates = 20`
- `missing_trade_dates = 0`
- `acceptance_pass = PASS`

说明：

- E_ret 学习窗口推进正常
- latest 工件能落盘
- 工程层已经通过

但从多轮验收与决策输出来看：

- 某些 run 中，E_ret 会被明显压缩
- 在较弱的 run 中，E_ret 头部只到 0.02 左右
- 在恢复正常的 run 中，E_ret 头部可回到 0.05 ~ 0.06+

说明：

**E_ret 当前不是“彻底坏掉”，而是“输出分布稳定性不足”。**

进一步结合 acceptance 文件判断：

- LR 有时方向性、相关性改善
- 但误差类指标未稳定变优
- LGBM 在若干轮中偏弱

结论：

- E_ret 工程链路已收口
- E_ret 模型效果层尚未收口
- 当前最大疑点是：**E_ret 在决策层前后的分布是否被压缩 / 缩放 / 波动放大**

### 4. learning acceptance 层状态

`outputs/learning/learning_acceptance_latest.md / .json` 已成功建立。

意义：

- 工程层验收与效果层验收已被分开
- 现在能明确区分：
  - 工程通过
  - 效果未必提升

但仍有一个规则问题未完全收口：

- 手动运行旧日期测试时，`latest acceptance` 仍可能被旧窗口覆盖

这个问题本质不是模型问题，而是 latest 命名治理问题。

---

## 三、站在系统最高价值视角的当前判断

### 最高价值仍然锁死

`top10-decision` 的最高价值不是：

- workflow 跑通
- 训练文件生成
- 指标表存在

而是：

**在 T 日收盘后，从候选池中输出真正有执行意义、并尽量具有正期望值的 T+1 组合。**

所以当前必须从全系统视角承认：

### 已经完成的升级

你已经把系统从：

- 只会跑流程
- latest 状态不可信
- 学习链不稳定

升级到：

- 学习链可执行
- latest / dated 工件可追溯
- P_fill / E_ret 验收层已建立
- 工程层状态透明化

### 还没有完成的升级

你还没有把系统升级到：

- P_fill 在 decision 层有足够区分度
- E_ret 在 decision 层输出稳定、可靠、可解释
- learning 结果对 EV / TopN 排序的真实贡献可被证实

所以当前状态不是“系统坏了”，而是：

**工程底座已经明显升级，但学习系统对决策层的真实价值还没有被完全收口。**

---

## 四、当前不要做的事

下次继续时，不要发散到这些方向：

- 不要再回头修 workflow 语法
- 不要再重复核对 latest meta 是否落盘
- 不要优先做 Node.js 24 兼容
- 不要优先重写整套 decision 引擎
- 不要优先改 Premium
- 不要再优先纠结手动旧日期测试覆盖 latest 这类次级治理问题

这些都不是当前第一主线。

---

## 五、当前唯一主线

### 主线名称

**学习系统对决策层真实价值收口**

### 当前第一优先问题

#### 问题 A：P_fill 为什么几乎全部封顶到 0.98
目标：

- 判断是模型原始输出本来就高度饱和
- 还是 decision 层做了 `clip / cap`
- 还是存在后处理把概率压成常数项

如果 P_fill 长期是 0.98 常数项，那么它对 EV 排序几乎退化成无区分度模块。

#### 问题 B：E_ret 为什么有时明显压缩，有时又恢复
目标：

- 判断是 E_ret 模型原始输出分布自身波动
- 还是 decision 层消费 E_ret 时做了缩放 / 裁剪
- 还是不同 run 的 latest 工件语义被测试运行干扰

当前最需要分清：

**E_ret 的不稳定，究竟发生在“模型输出端”，还是“decision 接线端”。**

---

## 六、下次进入后，优先核对的固定对象

以后下次直接围绕以下对象推进：

### 学习侧
- `models/pfill_meta.json`
- `models/eret_meta.json`
- `outputs/learning/learning_acceptance_latest.json`

### 决策侧
- `outputs/decision/decision_report_20260327.md`
- `outputs/decision/decision_report_20260330.md`
- `outputs/decision/decision_report_20260331.md`
- `outputs/decision/eval_20260327.json`
- `outputs/decision/eval_20260330.json`
- `outputs/decision/eval_20260331.json`

### 下一步最值得查的代码方向
- decision 层中消费 `P_fill` 的位置
- decision 层中消费 `E_ret` 的位置
- 是否存在 `clip / cap / normalize / shrink` 逻辑
- 是否存在把 `P_fill` 压到 0.98 的后处理
- 是否存在把 `E_ret` 压缩到窄区间的接线逻辑

---

## 七、下次进入后直接执行口令

下次进入 `top10-decision` 主线后，直接按下面口令继续：

继续 `top10-decision` 主线，不回退旧话题，不重做 workflow，不改上下游路径，不推倒重来。

当前阶段已经不是 learning 主链能不能跑，而是：

**学习系统对 decision 层的真实价值收口。**

当前状态锚点如下：

1. workflow / learning 主链已基本收口，`pfill_meta.json / eret_meta.json / learning_acceptance_latest` 已能产出并验收。
2. P_fill 工程层通过，但在 `decision_report_20260331.md` 中大量候选 `P_fill = 0.98`，疑似封顶，区分度不足。
3. E_ret 工程层通过，但决策层输出分布稳定性不足，部分 run 明显压缩，部分 run 又恢复到 0.05 ~ 0.06+。
4. 当前最大问题已不再是主链打不通，而是：
   - P_fill 是否在 decision 层退化成常数项
   - E_ret 的不稳定到底发生在模型端还是接线端

下次进入后，按以下顺序执行：

### 第一步：先审 decision 层输出，不发散新功能
优先对比：
- `decision_report_20260327.md`
- `decision_report_20260330.md`
- `decision_report_20260331.md`
- `eval_20260327.json`
- `eval_20260330.json`
- `eval_20260331.json`

目标：
- 判断 P_fill 是否长期被封顶到 0.98
- 判断 E_ret 是原始输出压缩，还是 decision 层压缩

### 第二步：只查当前可疑接线，不重建系统
重点查 decision 层中：
- P_fill 的消费位置
- E_ret 的消费位置
- clip / cap / shrink / normalize / risk coupling 逻辑

目标：
- 找到 P_fill 常数化的根因
- 找到 E_ret 波动/压缩的根因

### 第三步：在定位根因前，不做大改
当前不要优先处理：
- workflow 大改
- Premium
- Node.js 24
- 手动旧日期 latest 覆盖规则

---

## 八、当前一句话总结

当前 `top10-decision` 已从“learning 主链不稳定”的阶段，推进到“learning 工程层已基本收口，但 P_fill 区分度不足、E_ret 决策层输出稳定性不足，下一步需直接审 decision 接线与学习系统的真实有效性”的阶段。
