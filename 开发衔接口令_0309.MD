# 开发衔接口令_0309.MD

继续 top10-decision 主线开发，严格以《核心算法需求规范（Core Algorithm Spec).MD》为最高锚定，不跑偏。

---

## 一、当前主线已明确

# Decision 主线

不是历史补跑主线。
不是 Premium 展示主线。
当前只聚焦 Decision 五模块：

1. P_fill / 可买性模块
2. E_ret / 预期收益模块
3. Cost / 交易成本模块
4. RiskPenalty / 风险惩罚模块
5. EV / 综合价值模块

---

## 二、当前已完成状态

### 1. P_fill 学习链已打通
已完成：
- build_fill_truth.py
- build_pfill_trainset.py
- train_pfill.py
- models/pfill_*.joblib
- models/pfill_meta.json

已确认：
- 在线推理已真实接通
- data/decision/decision_candidates_YYYYMMDD.csv 中已出现：
  - p_fill_pred_model
  - p_fill_pred_final
  - p_fill_pred
  - p_fill_pred_src = model:lgbm

当前判断：
- P_fill 作为可买性变量，高位集中有业务合理性
- 不再仅凭大量接近 0.98 判异常

---

### 2. E_ret 学习链已打通
已完成：
- build_eret_truth.py
- build_eret_trainset.py
- train_eret.py
- models/eret_*.joblib
- models/eret_meta.json

已确认：
- 在线推理已真实接通
- data/decision/decision_candidates_YYYYMMDD.csv 中已出现：
  - eret_pred_model
  - eret_pred_final
  - eret_pred
  - e_ret_pred
  - eret_pred_src = model:lgbm

当前判断：
- E_ret 已开始出现横向分化
- 不再仅凭某次 TopN 局部接近判异常

---

### 3. Decision 主引擎接线已完成
已完成：
- src/top10decision/engines/pfill_engine.py
- src/top10decision/engines/eret_engine.py
- scripts/run_v2.py
- .github/workflows/run_decision_daily.yml

已确认：
- outputs/decision/decision_report_YYYYMMDD.md 中：
  - p_fill_pred_src = model:lgbm
  - eret_pred_src = model:lgbm
  - p_fill_degrade_reason = none
  - eret_degrade_reason = none

说明：
- 当前已不是接线问题
- 当前已进入下一阶段模块完善问题

---

## 三、当前已锁定理解

### 1. 判断层不能只看 TopN
不能只看：
- decision_report_YYYYMMDD.md
- TopN Targets

更可靠的判断层是：

- data/decision/decision_candidates_YYYYMMDD.csv

因为这张表已证明：
- P_fill 已真实计算
- E_ret 已真实计算
- EV 已真实计算

---

### 2. Decision 与 Premium 已明确分工

#### Decision
面向机器执行。
机器真正使用的是：
- docs/signals/*.csv
- docs/weights/*.csv

decision_report_YYYYMMDD.md 只是给人看的机器执行摘要，不是机器直接执行文件。

#### Premium
面向人类交易判断。
当前不展开，后续另起主线。

---

## 四、当前挂起但不作为主线的问题

### 1. 历史补跑 / 日期契约问题
包括：
- workflow_dispatch trade_date
- signal_date / exec_date
- 历史输出命名一致性
- a-top10 某些历史 pred_decisio_YYYYMMDD.csv 缺失

这些暂时挂起，不占当前主线。

### 2. Premium 展示设计问题
包括：
- 给人看的交易指导表
- 过去 10 个交易日测试统计报告
- Premium 报告结构

这些属于后续独立主线，当前不展开。

---

## 五、下一次最合理任务

下一次不要再回头纠缠：
- P_fill / E_ret 是否接通
- TopN 局部接近
- 历史补跑某一天报错
- Premium 该展示什么字段

这些都已不是当前第一优先级。

---

## 六、下一次主线正式切到

# Cost / RiskPenalty / EV 三模块

目标：

# 把 EV 链做实

顺序：

### 第一步：Cost
检查当前交易成本模块是否仍只是轻量占位逻辑。

### 第二步：RiskPenalty
检查当前风险惩罚模块是否仍只是轻量占位逻辑。

### 第三步：EV
在 Cost / RiskPenalty 做实后，再升级 EV，使其成为真正有交易意义的综合排序核心。

---

## 七、下一次代码第一刀

优先从：

- src/top10decision/models/costs.py

开始。

原因：
- Cost / RiskPenalty 当前大概率都在这里
- 先看清现状
- 再决定原地升级还是拆分模块

---

## 八、协作规则保持不变

1. 一个文件一个文件来
2. 每次先给：
   - 文件全路径
   - 简要修改报告
   - 完整可覆盖代码
3. 超过 300 行文件优先基于 Raw 链接修改
4. 全部使用简体中文
5. 始终从整个预测系统全局有效、有序执行出发

---

## 九、下次开新对话时可直接粘贴

继续 top10-decision 主线开发。

当前主线明确为 Decision 主线。
P_fill / E_ret 已完成收口：
1. 学习链已打通
2. 在线推理链已打通
3. decision_candidates 表已证明两者真实计算
4. P_fill 高位集中暂视为业务合理
5. E_ret 已开始横向分化
6. 不再只凭 TopN 局部接近判异常

历史补跑问题挂起，Premium 属于另一条后续主线。

下一任务切到：
Cost / RiskPenalty / EV 三模块，
目标是把 EV 链做实。
先从 src/top10decision/models/costs.py 开始。
