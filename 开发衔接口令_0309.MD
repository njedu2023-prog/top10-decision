继续 top10-decision 主线开发，严格以《核心算法需求规范（Core Algorithm Spec).MD》为最高锚定，不跑偏。

当前主线已明确：

# Decision 主线

不是历史补跑主线。  
不是 Premium 展示主线。  
当前只聚焦 Decision 五模块：

1. P_fill / 可买性模块
2. E_ret / 预期收益模块
3. Cost / 交易成本模块
4. RiskPenalty / 风险惩罚模块
5. EV / 综合价值模块

---

## 当前已完成状态

### 1. P_fill 学习链已打通
已完成：
- `build_fill_truth.py`
- `build_pfill_trainset.py`
- `train_pfill.py`
- `models/pfill_*.joblib`
- `models/pfill_meta.json`

已确认：
- 在线推理已真实接通
- `decision_candidates_YYYYMMDD.csv` 中已出现：
  - `p_fill_pred_model`
  - `p_fill_pred_final`
  - `p_fill_pred`
  - `p_fill_pred_src = model:lgbm`

当前判断：
- P_fill 作为可买性变量，高位集中有业务合理性
- 不再仅凭大量接近 `0.98` 判异常

---

### 2. E_ret 学习链已打通
已完成：
- `build_eret_truth.py`
- `build_eret_trainset.py`
- `train_eret.py`
- `models/eret_*.joblib`
- `models/eret_meta.json`

已确认：
- 在线推理已真实接通
- `decision_candidates_YYYYMMDD.csv` 中已出现：
  - `eret_pred_model`
  - `eret_pred_final`
  - `eret_pred`
  - `e_ret_pred`
  - `eret_pred_src = model:lgbm`

当前判断：
- E_ret 已开始出现横向分化
- 不再仅凭某次 TopN 局部接近判异常

---

### 3. Decision 主引擎接线已完成
已完成：
- `src/top10decision/engines/pfill_engine.py`
- `src/top10decision/engines/eret_engine.py`
- `scripts/run_v2.py`
- `.github/workflows/run_decision_daily.yml`

已确认：
- `decision_report_YYYYMMDD.md` 中：
  - `p_fill_pred_src = model:lgbm`
  - `eret_pred_src = model:lgbm`
  - `p_fill_degrade_reason = none`
  - `eret_degrade_reason = none`

说明：
- 当前已不是接线问题
- 当前进入下一阶段模块完善问题

---

## 当前已锁定理解

### 1. 判断层不能只看 TopN
不能只看：
- `decision_report_YYYYMMDD.md`
- `TopN Targets`

更可靠的判断层是：

```text
data/decision/decision_candidates_YYYYMMDD.csv

继续 top10-decision 主线开发。

当前主线明确为 Decision 主线。
P_fill / E_ret 已完成收口：
1. 学习链已打通
2. 在线推理链已打通
3. decision_candidates 表已证明两者真实计算
4. P_fill 高位集中暂视为业务合理
5. E_ret 已开始横向分化
6. 不再只凭 TopN 局部接近判异常

历史补跑问题挂起，Premium 属于另一条后续主线。

下一任务切到：
Cost / RiskPenalty / EV 三模块，
目标是把 EV 链做实。
先从 src/top10decision/models/costs.py 开始。
