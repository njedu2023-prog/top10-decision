# 开发衔接口令_0325

## 一、执行总原则（锁死）

继续 `top10-decision` 主线开发，不回退旧话题，严格按《交互契约.MD》执行，默认授权长期有效，尽量不中断。

后续同类问题默认规则：
- 只要属于主线开发推进、代码修改、文档更新、仓库内可逆/不可逆写入、提交流程、最小必要排查，默认已获授权
- 不再反复向用户询问“是否确认”“是否继续”“是否提交”
- 如遇真实阻塞，只汇报阻塞点与下一步最小动作，不泛泛讨论

---

## 二、系统最高目标（锁死）

`Top10 Decision` 的唯一最高目标，是在 **T日收盘后**，从候选池中输出一个 **可执行组合**，使其同时满足：

1. 在 **T+1** 具备可成交性  
2. 在 **T+1 → T+2** 具备正期望收益  
3. 整体风险可控  
4. 系统具备自学习与持续迭代优化能力，并且长期提升总目标价值

---

## 三、业务真值边界（锁死）

由于下游第三方真实成交、持仓、滑点、账户收益等数据 **不回流** 本系统，因此：

`top10-decision` 的验证与学习，**只能基于公开真值层可获得的到期数据完成**，不得以第三方真实业务收益作为训练标签或验收前提。

系统学习口径正式锁死为：

- **P_fill**：基于公开行情与固定执行规则定义的可成交代理标签
- **EntryPriceProxy_T+1**：基于公开行情与固定规则定义的代理买入价
- **E_ret**：基于 `EntryPriceProxy_T+1` 与 `T+2` 真值价格定义的代理收益标签
- **RiskPenalty**：基于公开行情定义的风险代理标签

---

## 四、不可破坏的工程约束（锁死）

后续一切修改都必须同时满足：

- 上游来的数据路径保持不改变
- 向下游输出的信号文件和路径不改变
- 完善提升过程中，不重建整条链路
- 定时任务执行不能被破坏
- 允许局部链路临时中断，但最终必须回到原主链并恢复稳定
- 原系统链路原本是正常能跑通的，当前开发是在其上做增量强化，不是推倒重来

---

## 五、本轮已完成锚定

### 1. 配置层锚定已建立
仓库已新增并启用：

- `src/top10decision/configs.py`

其中已锁定：

- `EntryPriceProxyConfig.mode_default = "t1_open"`
- `EntryPriceProxyConfig.mode_fallback = "t1_open_degrade_missing"`
- `FillTruthConfig` 承载 `buy_window_start / buy_window_end`
- `ERetTruthConfig` 使用 `EntryPriceProxy_T+1 + close_T2` 构造代理收益真值

### 2. `build_fill_truth.py` 已完成主线收口
已将 `scripts/build_fill_truth.py` 收口到统一配置口径：

- 不再硬编码旧枚举
- `entry_price_proxy_mode` 改为读取 `configs.py`
- 旧口径已统一替换为：
  - `"open_t1"` → `t1_open`
  - `"weak_daily_proxy"` → `t1_open_degrade_missing`
- `buy_window_start / buy_window_end` 已改为读取 `fill_truth_config`
- 未改输入输出路径
- 未改 workflow / signals / weights / docs/reports 对外契约

### 3. 兼容性判断已完成
已检查当前链路，确认：

- `build_pfill_trainset.py` 主要消费 `fill_truth` 结果字段
- `build_eret_truth.py` 主要复用 `fill_truth` 中的 `entry_price_proxy_t1`
- 当前主风险不是路径或 workflow，而是“字段口径是否一致、旧枚举是否残留”

---

## 六、当前系统视角小结

当前不再是“是否重建链路”的阶段，而是进入：

**公开真值代理执行语义收口阶段**

也就是把以下四个对象，在代码层彻底统一：

- `P_fill`
- `EntryPriceProxy_T+1`
- `E_ret`
- `RiskPenalty`

当前优先级不是扩新功能，不是重做上游，不是重做 workflow，而是：

1. 先把 `fill_truth -> pfill_trainset -> eret_truth -> eret_trainset` 的字段语义统一  
2. 再看训练链是否稳定  
3. 再考虑 audit / meta / degrade 记录增强  
4. 最后再进入进一步评估与优化

---

## 七、下一轮进入时的第一优先动作

下次进入本仓库主线后，**不要回退旧话题**，直接继续以下顺序执行：

### 第一步：检查 `build_pfill_trainset.py`
目标：

- 确认它消费 `fill_truth` 的字段口径与当前已更新的 `build_fill_truth.py` 完全一致
- 尤其检查：
  - 是否依赖 `entry_price_proxy_mode` 的旧枚举值
  - 是否假设旧字段名
  - 是否对 `buy_window_start / buy_window_end` 有隐含旧逻辑依赖

要求：

- 先做最小排查
- 若需改动，只做最小安全改动
- 不碰路径，不碰外部契约

### 第二步：检查 `build_eret_truth.py`
目标：

- 确认其对 `EntryPriceProxy_T+1` 的消费完全与当前配置层一致
- 确认没有残留旧枚举：
  - `open_t1`
  - `weak_daily_proxy`

重点检查：

- 是否只把 `entry_price_proxy_mode` 当普通记录字段
- 是否对 mode 值存在硬编码判断
- 是否对 degrade 行为存在口径分叉

### 第三步：检查 `build_eret_trainset.py / train_eret.py`
目标：

- 确认训练链只消费统一后的真值字段
- 不出现旧枚举兼容分叉
- 不因字段口径变化破坏原训练输出契约

---

## 八、本轮之后允许做的增强（但不是第一优先）

只有在主链口径统一完成后，才允许继续考虑以下增强：

### 1. audit / meta 增强
在不破坏现有文件契约的前提下，可考虑补充：

- degrade 行为计数
- `entry_price_proxy_mode` 分布
- 缺失真值比例
- fill_label_quality 分布

### 2. 文档契约同步
将仓库内涉及旧枚举的 MD 文档同步收口，避免文档和代码分叉。

注意：

- 文档更新属于必要收口
- 但优先级低于代码主链稳定

### 3. 历史 CSV 回补
历史 `fill_truth_*.csv` 中若仍残留旧枚举，不作为当前第一优先。
原则：

- 不强制回溯重写历史文件
- 新脚本生成的新文件自然统一新口径即可

---

## 九、输出要求（下次继续时照此执行）

每次推进后，都按以下结构汇报，不要泛泛而谈：

### 1）你改了哪个文件
明确到文件路径

### 2）为什么改
必须指向主线问题，不讲空话

### 3）完整可覆盖版代码
如果涉及代码修改，优先给完整可覆盖版，方便整体替换

### 4）下一步主线是什么
必须只给主线，不要发散

---

## 十、下次对话直接执行口令

下次进入本仓库主线后，直接按下面口令继续：

继续 `top10-decision` 主线，不回退旧话题，严格按《交互契约.MD》执行，默认授权长期有效，尽量不中断。

当前阶段不是重做 workflow，不是改上下游路径，不是推倒重来，而是继续完成“公开真值代理执行语义”的主链收口。

直接从以下顺序开始执行：

1. 检查 `scripts/build_pfill_trainset.py` 是否与 `build_fill_truth.py` 的新枚举口径完全一致  
2. 检查 `scripts/build_eret_truth.py` 是否对 `EntryPriceProxy_T+1 / entry_price_proxy_mode` 存在旧枚举硬编码  
3. 检查 `scripts/build_eret_trainset.py` 与 `scripts/train_eret.py` 是否仍残留旧口径依赖  
4. 若发现问题，只做最小安全改动，不改路径、不改外部契约、不破坏 workflow  
5. 做完后按“改了哪个文件 / 为什么改 / 完整可覆盖版 / 下一步主线”四段输出

补充默认规则：
- 以后同类问题默认：同意
- 只要属于主线推进所需的仓库写入、代码修改、文档更新、提交动作，默认授权有效
- 不再重复向用户索要确认
