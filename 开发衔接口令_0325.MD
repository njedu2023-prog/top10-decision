开发衔接口令_0325

一、执行总原则（锁死）

继续 top10-decision 主线开发，不回退旧话题，严格按《交互契约.MD》执行，默认授权长期有效，尽量不中断。

后续同类问题默认规则：

只要属于主线开发推进、代码修改、文档更新、仓库内可逆/不可逆写入、提交流程、最小必要排查，默认已获授权。

不再反复向用户询问“是否确认”“是否继续”“是否提交”。

如遇真实阻塞，只汇报阻塞点与下一步最小动作，不泛泛讨论。

二、系统最高目标（锁死）

Top10 Decision 的唯一最高目标，是在 T 日收盘后，从候选池中输出一个可执行组合，使其同时满足：

在 T+1 具备可成交性；  
在 T+1 → T+2 具备正期望收益；  
整体风险可控；  
系统具备自学习与持续迭代优化能力，并且长期提升总目标价值。

三、业务真值边界（锁死）

由于下游第三方真实成交、持仓、滑点、账户收益等数据不回流本系统，因此：

top10-decision 的验证与学习，只能基于公开真值层可获得的到期数据完成，不得以第三方真实业务收益作为训练标签或验收前提。

系统学习口径正式锁死为：

P_fill：基于公开行情与固定执行规则定义的可成交代理标签  
EntryPriceProxy_T+1：基于公开行情与固定规则定义的代理买入价  
E_ret：基于 EntryPriceProxy_T+1 与 T+2 真值价格定义的代理收益标签  
RiskPenalty：基于公开行情定义的风险代理标签

四、不可破坏的工程约束（锁死）

后续一切修改都必须同时满足：

上游来的数据路径保持不改变；  
向下游输出的信号文件和路径不改变；  
完善提升过程中，不重建整条链路；  
定时任务执行不能被破坏；  
允许局部链路临时中断，但最终必须回到原主链并恢复稳定；  
原系统链路原本是正常能跑通的，当前开发是在其上做增量强化，不是推倒重来。

五、本轮已完成锚定

1. 配置层锚定已建立

仓库已新增并启用：

`src/top10decision/configs.py`

其中已锁定：

`EntryPriceProxyConfig.mode_default = "t1_open"`  
`EntryPriceProxyConfig.mode_fallback = "t1_open_degrade_missing"`  
`FillTruthConfig` 承载 `buy_window_start / buy_window_end`  
`ERetTruthConfig` 使用 `EntryPriceProxy_T+1 + close_T2` 构造代理收益真值

2. 公开真值代理执行语义主链已收口

已完成并验证通过的主线修复包括：

- `scripts/build_fill_truth.py` 已收口到统一配置口径
- 不再硬编码旧枚举
- `entry_price_proxy_mode` 已读取 `configs.py`
- 旧口径已统一替换为：
  - `"open_t1"` → `t1_open`
  - `"weak_daily_proxy"` → `t1_open_degrade_missing`
- `buy_window_start / buy_window_end` 已改为读取 `fill_truth_config`

同时已修复两类主链硬伤：

第一类：成熟度字段名错配  
- `sample_maturity_latest.csv` 实际字段为 `PFILL_READY`
- `build_fill_truth.py` 旧写法误读为 `FILL_READY`
- 已修复为兼容读取 `PFILL_READY / FILL_READY`
- 结果：`pfill_trainset_20260324.csv` 已恢复为真实非空可训练样本

第二类：E_ret 历史依赖缺口  
- `build_eret_truth.py` 必须优先复用 `fill_truth / entry_price_proxy_t1`
- 旧 workflow 未保证 E_ret 对应历史 `trade_date` 的 `pred_source` 先同步
- 已在 `run_decision_daily.yml` 中补齐 E_ret 分支的历史 `pred_source` 快照同步
- 结果：`pred_source_20260323.csv`、`fill_truth_20260323.csv`、`eret_truth_20260323.csv`、`eret_trainset_20260323.csv` 已真实落库

3. workflow 主链已验证成功

已确认：

- `Run Top10 Decision (Auto Daily) #106` 已成功跑绿
- `pfill_trainset_20260324.csv` 非空
- `eret_trainset_20260323.csv` 非空
- `pred_source_20260323.csv` 已落库到 `data/pred/archive/`
- `fill_truth_20260323.csv` 非空
- `eret_truth_20260323.csv` 非空
- `models/pfill_meta.json` 状态为 trained
- `models/eret_meta.json` 状态为 trained

六、当前系统视角小结

当前已经不再处于“公开真值代理执行语义是否能打通”的阶段，而是进入：

主链收口完成后的稳定化与固化阶段

也就是：

P_fill、EntryPriceProxy_T+1、E_ret、RiskPenalty 这四个对象，在代码层已完成一轮真正可运行、可落盘、可训练的主链闭合。

当前系统状态不是“表面 workflow 跑绿”，而是已经做到：

truth 层落盘  
trainset 层落盘  
model / meta 层落盘

当前优先级不再是继续回头折腾基础通路，不再是怀疑旧枚举主问题，也不再是重做 workflow，而是：

先把这次跑通后的验收基线固定下来；  
确认日常学习链推进机制可持续稳定；  
再考虑 audit / meta / degrade 记录增强；  
最后再进入下一阶段评估与优化。

七、下一轮进入时的第一优先动作

下次进入本仓库主线后，不要回退旧话题，直接继续以下顺序执行：

第一步：验收 truth 层与 model/meta 层最终落盘

目标：

确认这次不是只把 trainset 跑出来，而是 truth / trainset / model / meta 四层都真正稳定落盘。

优先检查：

- `data/market/fill_truth_20260323.csv`
- `data/market/eret_truth_20260323.csv`
- `models/pfill_meta.json`
- `models/eret_meta.json`
- `models/pfill_*.joblib`
- `models/eret_*.joblib`

要求：

先做最小验收  
不发散到新功能  
若发现问题，只做最小安全修复

第二步：检查 `sample_maturity_latest.csv` 推进机制是否持续稳定

目标：

确认 maturity 解析不是“这次偶然跑通”，而是能自然推进到后续交易日。

重点检查：

- `PFILL_READY / ERET_READY / FULLY_READY` 口径是否持续稳定
- `trade_date / exec_date / target_date` 推进是否一致
- 是否仍存在未来某天会再次断链的风险

第三步：固化主链验收基线

目标：

把当前已经证明有效的验收对象固定成后续标准基线。

建议固定基线：

- `sample_maturity_latest.csv`
- `fill_truth_{trade_date}.csv`
- `pfill_trainset_{trade_date}.csv`
- `eret_truth_{trade_date}.csv`
- `eret_trainset_{trade_date}.csv`
- `models/pfill_meta.json`
- `models/eret_meta.json`

第四步：只在主链稳定后再处理次级工程项

这些不是第一优先，但可以在主链彻底稳定后再看：

- GitHub Actions Node.js 20 deprecation warning 升级到 Node 24
- 文档中旧字段名/旧口径的进一步同步收口
- learning workflow 的日志和审计增强

八、本轮之后允许做的增强（但不是第一优先）

1. audit / meta 增强

在不破坏现有文件契约的前提下，可考虑补充：

- degrade 行为计数
- entry_price_proxy_mode 分布
- 缺失真值比例
- fill_label_quality 分布
- maturity 推进统计

2. 文档契约同步

将仓库内涉及旧字段名、旧枚举、旧描述的 MD 文档同步收口，避免文档和代码再次分叉。

注意：

文档更新属于必要收口；  
但优先级低于主链稳定。

3. Node.js 24 兼容升级

当前 workflow 已跑通，但仍有 GitHub Actions Node.js 20 deprecation warning。  
这属于后续工程维护项，不阻塞当前主链。

九、输出要求（下次继续时照此执行）

每次推进后，都按以下结构汇报，不要泛泛而谈：

1）改了哪个文件  
明确到文件路径；若没改文件，就直接写“本轮未改文件”。

2）为什么改  
必须指向主线问题，不讲空话。

3）完整可覆盖版  
如果涉及代码或 workflow 修改，优先给完整可覆盖版，方便整体替换。

4）下一步主线是什么  
必须只给主线，不要发散。

十、下次对话直接执行口令

下次进入本仓库主线后，直接按下面口令继续：

继续 top10-decision 主线，不回退旧话题，严格按《交互契约.MD》执行，默认授权长期有效，尽量不中断。

当前阶段不是重做 workflow，不是改上下游路径，不是推倒重来，而是已经完成“公开真值代理执行语义”的主链收口，接下来进入收口后的稳定化验收与固化阶段。

直接从以下顺序开始执行：

第一步：验收 truth 层与 model/meta 层最终落盘  
优先检查：
- data/market/fill_truth_20260323.csv
- data/market/eret_truth_20260323.csv
- models/pfill_meta.json
- models/eret_meta.json
- models/pfill_*.joblib
- models/eret_*.joblib

第二步：检查 sample_maturity_latest.csv 推进机制是否持续稳定

第三步：固化主链验收基线，后续按固定对象验收，不再重复大范围排查

若发现问题，只做最小安全改动，不改路径、不改外部契约、不破坏 workflow。

做完后按“改了哪个文件 / 为什么改 / 完整可覆盖版 / 下一步主线”四段输出。

补充默认规则：

以后同类问题默认：同意。  
只要属于主线推进所需的仓库写入、代码修改、文档更新、提交动作，默认授权有效。  
不再重复向用户索要确认。
