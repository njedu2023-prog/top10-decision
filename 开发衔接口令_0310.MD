# 开发衔接口令_0310.MD

继续 top10-decision 主线开发，严格以《核心算法需求规范（Core Algorithm Spec).MD》为最高锚定，不跑偏。

---

## 一、当前主线已切换

**Premium 主线**

不是 Decision 主线继续深挖。  
不是历史补跑主线。  
不是展示层小修小补。

当前正式切到：

- Premium / 人类交易判断主线

---

## 二、Decision 主线当前状态（作为已完成锚点保留）

Decision 主线当前判断：

**已基本收口。**

已完成的核心内容包括：

### 1. P_fill 学习链已打通
已完成：

- build_fill_truth.py
- build_pfill_trainset.py
- train_pfill.py
- models/pfill_*.joblib
- models/pfill_meta.json

已确认：

- 在线推理已真实接通
- `decision_candidates_YYYYMMDD.csv` 中已出现：
  - `p_fill_pred_model`
  - `p_fill_pred_final`
  - `p_fill_pred`
  - `p_fill_pred_src = model:lgbm`

当前判断：

- P_fill 作为可买性变量，高位集中有业务合理性
- 不再仅凭大量接近 0.98 判异常

---

### 2. E_ret 学习链已打通
已完成：

- build_eret_truth.py
- build_eret_trainset.py
- train_eret.py
- models/eret_*.joblib
- models/eret_meta.json

已确认：

- 在线推理已真实接通
- `decision_candidates_YYYYMMDD.csv` 中已出现：
  - `eret_pred_model`
  - `eret_pred_final`
  - `eret_pred`
  - `e_ret_pred`
  - `eret_pred_src = model:lgbm`

当前判断：

- E_ret 已开始出现横向分化
- 不再仅凭某次 TopN 局部接近判异常

---

### 3. Decision 主引擎接线已完成
已完成：

- `src/top10decision/engines/pfill_engine.py`
- `src/top10decision/engines/eret_engine.py`
- `scripts/run_v2.py`
- `.github/workflows/run_decision_daily.yml`

已确认：

- `outputs/decision/decision_report_YYYYMMDD.md` 中：
  - `p_fill_pred_src = model:lgbm`
  - `eret_pred_src = model:lgbm`
  - `p_fill_degrade_reason = none`
  - `eret_degrade_reason = none`

说明：

- 当前已不是接线问题
- 当前已不是“能不能跑”的问题
- 当前已进入收口完成后的下一主线阶段

---

### 4. Cost / RiskPenalty / EV 已基本做实
已确认：

- 已从占位常数升级为逐股规则计算
- EV 已真实吃到 Cost / RiskPenalty
- `decision_candidates_YYYYMMDD.csv` 已证明三者真实计算
- cost_* 分项已落表
- risk_* 分项已落表
- 第二轮 RiskPenalty 聚合逻辑已进入产物
- `decision_report_YYYYMMDD.md` 已可输出：
  - TopN Targets
  - Full Candidate Pool

当前判断：

- Decision 已不是概念框架
- 已形成真实可输出、可排序、可交易解释的系统雏形

---

### 5. Decision 主线当前最终定性
Decision 主线当前结论：

**主体开发已基本完成，已进入“阶段性收口完成”状态。**

含义：

- 允许后续继续优化
- 但当前不再把主要开发资源放在 Decision 内部深挖
- 当前主线切换到 Premium

---

## 三、当前已锁定理解

### 1. Decision 与 Premium 已明确分工

#### Decision
面向机器执行。  
机器真正使用的是：

- `docs/signals/*.csv`
- `docs/weights/*.csv`

`decision_report_YYYYMMDD.md` 只是给人看的机器执行摘要，不是机器直接执行文件。

#### Premium
面向人类交易判断。  
它不是机器执行文件。  
它的价值重点是：

- 给人看的交易解释
- 给人看的价格/收益判断
- 给人看的候选比较与分位分布
- 让人类在 T+1 执行时拥有更强的信息优势与解释优势

---

### 2. E_ret 与 Premium 的关系已明确
当前理解锁定：

- E_ret 不是裸价格本身
- E_ret 更接近“锚定目标时点的预期收益率”
- 对当前业务链可近似理解为：
  - `20260309` 收盘后生成表
  - `20260310` 执行
  - E_ret 主要锚定到后续目标时点（当前可近似理解为模拟到 `20260311` 收盘附近的收益判断）

因此：

- Premium 后续若要做人类可读价格/收益解释
- 需要围绕 E_ret 的业务含义展开
- 不能把 Premium 简化成 Decision 表的外观重画

---

### 3. Decision 当前挂起但不作为主线的问题

#### （1）历史补跑 / 日期契约问题
包括：

- workflow_dispatch trade_date
- signal_date / exec_date
- 历史输出命名一致性
- 某些历史 pred / outputs 缺失

这些暂时挂起，不占当前主线。

#### （2）Decision 内部继续深挖的小问题
包括：

- ST 样本自然触发不足
- tail risk 触发样本不足
- 权重后续如何做分层优化
- 展示层字段继续美化

这些都不作为当前第一优先级。

---

## 四、下一次正式主线

### Premium 主线

当前不要再继续深挖：

- P_fill 是否接通
- E_ret 是否接通
- RiskPenalty 是否继续精修
- Decision 再加哪些表
- 历史补跑某一天报错

这些都已不是下一次的主线。

---

## 五、下一次 Premium 主线的目标

目标不是做“好看页面”这么浅。  
目标是：

### 把 Premium 做成真正面向人类交易判断的高价值输出层。

核心方向：

1. 明确 Premium 的最终业务口径  
2. 明确 Premium 与 Decision 的边界  
3. 明确 Premium 该吃哪些底层产物  
4. 明确 Premium 最终输出结构  
5. 明确 Premium 报告中“价格 / 收益 / 分位 / 解释”这几层如何组织

---

## 六、下一次最合理任务顺序

### 第一步：先重新锁定 Premium 的核心业务定义
要先回答清楚：

- Premium 到底主要服务什么决策动作？
- 是服务“人类 T+1 是否买”？
- 还是服务“人类如何理解这只票未来短周期的价格空间与收益空间”？
- 是以价格为主口径，还是以收益率为主口径？
- 它与 Decision 的重叠边界在哪里？

这一步先定清楚，后面开发才不会漂。

---

### 第二步：再梳理 Premium 该直接吃哪些底层字段
当前可重点考虑：

- P_fill
- E_ret
- EV
- Cost
- RiskPenalty
- 分位分布相关产物
- 候选池比较结果
- TopN / Full Candidate Pool 中对人有解释价值的字段

目标不是全搬，而是筛出：

**真正对人类判断有价值的字段。**

---

### 第三步：再定 Premium 最终输出结构
输出结构要先定，再写代码。  
包括：

- 预测表
- 验证表
- 人类解释段
- 关键指标段
- 风险提示段
- 候选比较段
- 后续可扩展模块

这一步属于 Premium 主线真正的框架搭建。

---

## 七、下一次代码第一刀

优先从：

### `Premium.md`

开始。

原因：

- 当前先不是马上改代码
- 而是先把 Premium 主线的业务契约重新锁死
- 先把业务口径、边界、结构、字段、价值目标写清楚
- 再决定改 `predict.py`、`report_md.py`、`io.py` 或其它文件

---

## 八、协作规则保持不变

- 一个文件一个文件来
- 每次先给：
  - 文件全路径
  - 简要修改报告
  - 完整可覆盖代码
- 超过 300 行文件优先基于 Raw 链接修改
- 全部使用简体中文
- 始终从整个预测系统全局有效、有序执行出发

---

## 九、下次开新对话时可直接粘贴

继续 top10-decision 主线开发。

当前主线已从 Decision 切换到 Premium。

Decision 当前已基本收口：
- P_fill 学习链已打通
- E_ret 学习链已打通
- Decision 主引擎接线已完成
- Cost / RiskPenalty / EV 已基本做实
- decision_report 已可输出 TopN Targets + Full Candidate Pool
- Decision 当前定性为：主体开发已基本完成，阶段性收口完成

当前不要再回头深挖：
- Decision 内部小修小补
- 历史补跑问题
- ST / tail risk 个别样本触发问题
- Premium 之外的展示层问题

下一次正式主线：
Premium 主线。

下一次第一任务：
先重写 / 锁定 `Premium.md`，
把 Premium 的核心业务定义、与 Decision 的边界、应吃的底层字段、最终输出结构先定清楚，
再进入后续代码开发。
