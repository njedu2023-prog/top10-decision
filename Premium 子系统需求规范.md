# Premium 子系统需求规范（Premium Contract V3）

适用仓库：`top10-decision`

当前主线状态：**Premium 主线正式升级为 E_ret_plus 主线**

本文件用途：锁定 Premium 子系统的核心目标、系统定位、与 Decision 的边界、核心预测对象 `E_ret_plus`、增强层 `X` 的定义、训练闭环方向、输出契约与后续工程开发顺序。

优先级说明：

本文件为 Premium 主线最高业务契约之一。  
如与局部实现、旧版 Premium 报告逻辑、展示层习惯、历史结构冲突，应优先以本文件为准修正实现，而不是反向扭曲主线目标。

---

# 一、当前主线切换结论

当前主线已从 **Decision 主线** 正式切换到 **Premium 主线**。

Decision 当前定性：

- `P_fill` 学习链已打通
- `E_ret` 学习链已打通
- Decision 主引擎接线已完成
- `Cost / RiskPenalty / EV` 已基本做实
- `decision_report` 已可输出 `TopN Targets + Full Candidate Pool`
- 当前判断为：**主体开发已基本完成，阶段性收口完成**

因此，当前不再把主要开发资源持续投入以下方向：

- Decision 内部小修小补
- 历史补跑问题
- ST / tail risk 个别样本触发问题
- Premium 之外的展示层问题
- 仅改善“人类看起来更舒服”的弱增量改造

当前正式主线：

**Premium 主线**

但 Premium 主线的定义已升级，不再是传统“报告层主线”，而是：

**以 `E_ret` 为第一预测核，构建 `E_ret_plus` 二级增强预测核的主线。**

---

# 二、Premium 的重新定义

## 2.1 Premium 不再是“人类报告层”主导系统

Premium 仍然服务于人类交易判断，但这不是它的最高价值。

当前重新定义如下：

**Premium 是 top10-decision 中围绕 `E_ret` 构建的二级收益增强系统。**

它的第一目标不是：

- 页面更好看
- 表达更方便
- 表格更丰富
- 报告更像产品页面

这些价值存在，但当前重要度仅约 **10%**，不构成主线。

Premium 的第一目标是：

**创造比原始 `E_ret` 更强、更准、更可闭环优化的最终收益判断对象：`E_ret_plus`。**

因此：

- 展示层不是 Premium 的主核
- 解释层不是 Premium 的主核
- 人类可读性不是 Premium 的主核
- 真正主核是：**预测增强**

---

## 2.2 Premium 的最高系统目标

Premium 的最高系统目标不是简单解释 Decision 结果，而是：

**在既有 Decision 成果基础上，围绕 `E_ret` 挖出真正新增量价值。**

这个新增量价值必须满足以下标准：

1. 不是对旧信息的重排
2. 不是把 `E_ret` 机械展开成伪分布
3. 不是把多个字段做一个花哨综合分
4. 不是仅做报告的“可读性升级”
5. 必须对预测本体、收益判断质量、决策质量带来真实增益

因此，Premium 的核心价值应被锁定为：

**把原始 `E_ret` 升级为更贴近真实交易世界、更适合人类执行判断的 `E_ret_plus`。**

---

# 三、Premium 与 Decision 的边界（重写版）

## 3.1 Decision 的职责

Decision 面向机器执行。

Decision 的职责是：

- 从候选池中做机器化排序
- 计算机器执行所需核心分数
- 产出可交易候选、可交易权重、TopN Targets
- 输出机器执行摘要
- 为聚宽或后续自动执行链提供底层可执行信息

机器真正使用的主要是：

- `docs/signals/*.csv`
- `docs/weights/*.csv`

`decision_report_YYYYMMDD.md` 只是给人看的机器执行摘要，  
**不是机器直接执行文件。**

Decision 当前已具备的核心价值包括：

- 排序
- 可买性
- 收益中心估计
- 成本
- 风险折减
- EV 级综合评价
- 候选池输出

Decision 当前定性为：

**第一层交易决策核已基本建成。**

---

## 3.2 Premium 的职责

Premium 不再定义为“Decision 的人类解释层”，而应定义为：

**Decision 之上的第二层收益增强核。**

它的职责不是复制 Decision，而是：

- 以 `E_ret` 为核心起点
- 基于真实交易语境对 `E_ret` 进行二次增强
- 产出 `E_ret_plus`
- 在此基础上再派生出分布表达、解释层、验证层与人类报告

因此，Premium 的职责应锁定为：

1. 生产比原始 `E_ret` 更强的最终收益判断对象
2. 建立 `E_ret` 的闭环增强链
3. 将已有 `P_fill / Cost / RiskPenalty / EV / T_Close / raw上下文` 作为增强器围绕 `E_ret` 服务
4. 把“收益预测”升级为“可相信的收益判断”
5. 在增强核之上，才构建报告、解释、分布与比较结构

---

## 3.3 二者边界的最终锁定

### Decision
决定：

**机器更想买谁**

### Premium
决定：

**机器当前给出的 `E_ret`，最终该被增强、维持还是打折，从而形成更强的收益判断核 `E_ret_plus`**

换言之：

- Decision 的核心对象：候选排序与机器执行价值
- Premium 的核心对象：收益预测增强与收益判断升级

---

# 四、Premium 的第一核心对象：E_ret

## 4.1 当前锁定认识

`E_ret` 是 Premium 的第一核心核。  
这一点必须锁死，不允许在 Premium 内部被稀释为众多字段中的普通一项。

当前锁定理解：

**`E_ret` 不是裸价格本身。**  
**`E_ret` 更接近锚定目标时点的预期收益率中心判断。**

在当前业务链中，`E_ret` 可近似理解为：

- `T日` 收盘后生成
- `T+1` 人类执行
- 面向后续目标时点（当前近似 `T+2` 收盘附近）的收益预测中心

因此，`E_ret` 回答的是：

**“这只票在目标时点，大概能给出多少收益率中心值？”**

这是有价值的，但还不够。

---

## 4.2 E_ret 的局限

原始 `E_ret` 虽然重要，但仍存在天然局限：

1. 它更像中心判断，不等于最终判断
2. 它不能自动表达“这次预测值有多可信”
3. 它不能自动表达“这个收益在当前真实交易语境下是否应打折理解”
4. 它不能自动吸收原始世界与交易约束对收益兑现的修正
5. 它不能天然代表“人类最终应相信多少收益空间”

因此，Premium 当前的主线，不是重复展示 `E_ret`，而是：

**继续围绕 `E_ret` 向上长出第二层核。**

---

# 五、Premium 的最终核心对象：E_ret_plus

## 5.1 E_ret_plus 的正式定义

`E_ret_plus` 定义为：

**在真实交易语境下，对原始 `E_ret` 进行条件增强、残差校准与可信修正后得到的最终收益判断核。**

它不是：

- `E_ret` 的重命名
- `E_ret` 的展示版
- `E_ret` 的分位图
- 多字段拼接后的综合分
- 另一条完全脱离 `E_ret` 的新预测线

它是：

**基于 `E_ret`，但强于 `E_ret` 的二级预测核。**

---

## 5.2 E_ret_plus 回答的不是原问题

### 原始 E_ret 回答
“目标时点预期收益率中心大概是多少？”

### E_ret_plus 回答
“在当前真实市场位置、交易约束、兑现条件、历史经验和模型偏差背景下，这个收益判断最终应被相信到什么程度？”

因此，`E_ret_plus` 不只是“收益预测”，而是：

**可相信收益判断**

这才是 Premium 的核心对象。

---

## 5.3 E_ret_plus 的存在意义

`E_ret_plus` 的存在意义不是让报告更漂亮，而是：

1. 让收益判断更接近真实交易世界
2. 让原始 `E_ret` 的系统性高估/低估得到修正
3. 让交易约束与市场现实进入收益判断主链
4. 让闭环学习真正创造新增量价值
5. 让 Premium 在 Decision 之上建立独立的高价值预测层

---

# 六、X 的正式定义：E_ret 二级增强层

## 6.1 X 不是字段，X 是系统层

当前主链定义为：

**Raw → T_Close → E_ret → X → E_ret_plus**

其中的 `X` 不应被理解成一个单独字段或一组随意拼接的特征。

`X` 应正式定义为：

**围绕 `E_ret` 构建的二级增强学习层**

建议命名：

- `EHX`
- `E_ret Enhancer`
- `E_ret Booster`
- `E_ret Residual Calibrator`

命名可后续统一，但逻辑必须锁死。

---

## 6.2 X 的唯一职责

`X` 的唯一职责是：

**学习原始 `E_ret` 在不同真实交易语境下的偏差、可信度与条件修正，从而产出增强后的 `E_ret_plus`。**

它不负责：

- 重做一遍完整收益预测
- 脱离 `E_ret` 另起炉灶
- 代替所有其他模块
- 承担页面渲染责任

它只负责一件事：

**把原始 `E_ret` 升级成更强的最终收益判断。**

---

# 七、X 的输入层级定义

`X` 必须围绕当前系统已具备或接近具备的现实要素构建。

## 7.1 第一层：核心核输入

这层不能缺失：

- `E_ret`
- `T_Close`

其中：

- `E_ret` 是第一收益核
- `T_Close` 是现实价格锚点

---

## 7.2 第二层：执行修正输入

这些字段不是 Premium 第一核心，但必须作为 `E_ret` 的增强器进入 `X`：

- `P_fill`
- `Cost`
- `RiskPenalty`
- `EV`

作用：

- 判断收益是否容易落地
- 判断收益是否会被交易摩擦吃掉
- 判断收益是否应被风险折价理解
- 判断收益、成本、风险三者是否一致

注意：

这些字段在 Premium 中的地位不应与 `E_ret` 抢主核，  
它们的正确位置是：

**服务于 `E_ret_plus` 的增强器。**

---

## 7.3 第三层：位置与状态输入

这层来自 `Raw + T_Close` 所代表的真实世界上下文，包括但不限于：

- T日涨跌幅
- T日振幅
- T日实体强弱
- 当前 close 在近N日区间中的位置
- 当前价格是否处于短期高位/拥挤位
- 当日量价状态
- 候选池相对位置
- 市场状态摘要
- 风格 / 情绪 / 板块状态摘要

作用：

**把 `E_ret` 放回真实的当日市场位置里。**

---

## 7.4 第四层：样本经验输入

这层是闭环增强必须具备的高价值层，包括但不限于：

- 历史同类样本中 `E_ret` 的偏差
- 类似条件下模型高估/低估倾向
- 当前样本成熟度
- 当前样本置信层级
- 历史误差桶 / 残差桶 / 条件偏差结构

作用：

**让 Premium 的增强不是拍脑袋修正，而是建立在闭环样本经验之上。**

---

# 八、X 的学习目标定义

## 8.1 X 不应直接重做未来收益预测

这条必须锁死。

如果 `X` 再去直接独立预测一次未来收益，就会和原始 `E_ret` 主线打架，造成：

- 模型重复
- 系统发散
- 解释困难
- 闭环不清楚
- 责任边界混乱

因此，`X` 的目标不应是“重新预测收益”，而应是：

**学习原始 `E_ret` 的条件偏差。**

---

## 8.2 X 的核心目标：学习残差修正

当前最合理、最干净、最可闭环的定义是：

`delta_ret = y_true_ret - e_ret_pred`

其中：

- `y_true_ret` = 真实目标收益率
- `e_ret_pred` = 原始 `E_ret` 预测值

则 `X` 的核心学习目标是：

**在当前条件下，原始 `E_ret` 会偏高还是偏低，偏多少。**

最终：

`E_ret_plus = E_ret + delta_hat`

这是当前 Premium 主线最专业、最现实、最不漂移的结构。

---

## 8.3 为什么采用残差校准路线

因为它同时满足：

1. 不推翻既有 `E_ret` 学习链
2. 真正围绕 `E_ret` 做增强，而不是另起一条混乱主线
3. 能形成闭环
4. 能显著利用历史样本经验
5. 能给出真实新增量价值

---

# 九、E_ret_plus 的输出契约

`E_ret_plus` 不应只输出一个裸值。

更合理的第一目标是三件套：

## 9.1 eret_plus_value
增强后的最终收益判断值

## 9.2 eret_plus_delta
相对原始 `E_ret` 的修正量

## 9.3 eret_plus_conf
当前增强结果的可信度 / 置信度 / 稳定度

这样可以同时回答：

- 原始 `E_ret` 是多少
- 为什么被调高或调低
- 调整幅度有多大
- 这次增强值应被相信多少

---

# 十、P10 / P50 / P90 的位置重写

## 10.1 分位表达不再作为主线起点

过去如果直接从 `E_ret` 机械展开 `P10 / P50 / P90`，其价值主要只是展示价值，不构成当前主线。

因此，当前锁定：

- `P10 / P50 / P90` 不是 Premium V3 的起点
- 不是先做的对象
- 不是主线核心

---

## 10.2 分位表达的正确位置

`P10 / P50 / P90` 的正确位置应是：

**E_ret_plus 成熟后在其之上的分布表达层。**

也就是说：

- 先有更强的 `E_ret_plus`
- 再谈更有业务意义的分位表达

否则分位很容易退化为伪分布、伪高级展示。

---

## 10.3 分位表达的条件

只有当以下任一条件成立，分位表达才具备真实业务价值：

1. `E_ret_plus` 已相对稳定
2. 已有足够残差样本支持条件分布估计
3. 已能对增强后的收益判断建立真实误差分布

在此之前，分位表达不应作为主线优先事项。

---

# 十一、P_fill / Cost / RiskPenalty 的位置重写

这三者仍然重要，但在 Premium V3 中应被重新降位并重新安放：

## 11.1 它们不是第一核心

Premium V3 中的第一核心只有：

- `E_ret`
- `E_ret_plus`

`P_fill / Cost / RiskPenalty` 不再平权并列为“同等级核心”。

---

## 11.2 它们的正确位置

它们的正确位置是：

**作为 `X` 的执行修正器、兑现约束器、风险折减器，围绕 `E_ret_plus` 服务。**

### P_fill
服务于：

- 收益是否容易兑现
- 机会是否容易落地

### Cost
服务于：

- 收益会被吃掉多少
- 交易摩擦有多强

### RiskPenalty
服务于：

- 原始收益是否应折价理解
- 为什么不能对高收益值盲目乐观

因此，它们应从“并列展示字段”转入“增强层输入 + 下游解释层”。

---

# 十二、Premium 的输出结构（重写版）

Premium 的输出结构不再由“好看报告”主导，而由“强预测核 + 从属表达层”主导。

## 12.1 模块 A：增强核摘要

必须优先输出：

- `trade_date`
- `exec_date`
- `target_date`
- `E_ret`
- `E_ret_plus`
- `eret_plus_delta`
- `eret_plus_conf`

这是 Premium V3 的第一输出层。

---

## 12.2 模块 B：增强结果主表

主表核心应围绕：

- 候选标的
- 原始 `E_ret`
- 增强后 `E_ret_plus`
- 修正方向 / 修正量
- 可信度
- 必要的执行修正摘要

主表的第一意义不再是“展示很多字段”，而是：

**呈现增强前后差异。**

---

## 12.3 模块 C：执行修正层

这一层展示但不抢主核，包括：

- `P_fill`
- `Cost`
- `RiskPenalty`
- `EV`
- 执行可实现性摘要
- 风险折减摘要

其作用是解释：

**为什么 `E_ret` 被增强、维持或打折。**

---

## 12.4 模块 D：验证闭环层

必须保留并强化：

- `premium_verify_YYYYMMDD.csv`
- 验证状态
- 实际命中情况
- `E_ret` 与 `E_ret_plus` 的后验对比
- 闭环误差记录

Premium 若不能闭环验证，就无法持续产生高价值。

---

## 12.5 模块 E：从属人类解释层

这一层存在，但当前重要度仅约 10%。

作用：

- 帮人快速理解增强逻辑
- 解释修正方向
- 说明当前机会更偏“可信”还是“脆弱”

注意：

这层不能反向主导 Premium 结构。

---

# 十三、Premium 的工程路线（重写版）

当前开发顺序必须调整。

## 第一步：先锁定 E_ret_plus 契约
必须先明确：

- `E_ret_plus` 是什么
- `X` 是什么
- `delta_ret` 如何定义
- `E_ret_plus` 输出什么
- 哪些字段进入增强层

在这一步没锁死前，不能继续停留在老版“报告字段讨论”。

---

## 第二步：实现 X / EHX 的最小可运行版
优先目标：

- 基于 `E_ret` 做残差校准
- 输出 `eret_plus_value / eret_plus_delta / eret_plus_conf`
- 跑通最小闭环

这是 Premium 当前的第一工程刀口。

---

## 第三步：重构输出表结构
此时再去改：

- `premium_top30_YYYYMMDD.csv`
- `premium_full_YYYYMMDD.csv`
- `premium_verify_YYYYMMDD.csv`
- `premium_report_YYYYMMDD.md`

但这些改造必须围绕 `E_ret_plus` 展开，而不是继续沿用旧版“字段堆叠式报告”思路。

---

## 第四步：再考虑分布与高级解释
只有在 `E_ret_plus` 跑稳后，才进入：

- P10 / P50 / P90
- 样本分层
- 高级人类解释
- ChatGPT 增值说明层

---

# 十四、Premium 的代码改造顺序（重写版）

当前第一改造文件应锁定为：

**`src/top10decision/premium/predict.py`**

原因：

- `E_ret_plus` 是预测核，不是报告核
- `X` / `EHX` 的第一落点应在预测层
- 不应从 `report_md.py` 这种下游展示层起刀

当前推荐改造顺序：

1. `src/top10decision/premium/predict.py`
2. `src/top10decision/premium/io.py`
3. `src/top10decision/premium/report_md.py`

必要时后续新增：

- 训练脚本
- 残差样本构建脚本
- 增强验证脚本

---

# 十五、当前挂起但不进入主线的问题

以下问题当前允许挂起，不占 Premium 第一优先级：

- Decision 内部小修小补
- 历史补跑 / 日期契约问题
- ST / tail risk 个别样本触发问题
- Premium 之外的展示层修饰问题
- 报告观感优化
- UI / 文案 / 样式层细节
- 机械式分位展示

---

# 十六、后续实现总原则（重写版）

1. 始终从整个预测系统全局有效、有序执行出发  
2. Premium 的第一价值是更强更准的预测，不是更好看的报告  
3. 所有结构都必须围绕 `E_ret -> E_ret_plus` 主链展开  
4. `P_fill / Cost / RiskPenalty / EV` 必须围绕 `E_ret_plus` 服务，而不是抢主核  
5. 先造强预测核，再造分布层，再造解释层  
6. 不做伪分布，不做伪高级展示，不做字段堆叠式假增量  
7. 必须形成真实闭环，让增强层能持续学习原始 `E_ret` 的偏差  
8. 实现上优先考虑技术现实与系统可运行性，不追求概念堆砌  
9. 所有后续实现若与本文件冲突，应优先修改实现，不应反向污染契约

---

# 十七、当前阶段最终结论

当前阶段对 Premium 的最终锁定结论如下：

- Premium 主线已从“人类报告层优化”升级为“`E_ret_plus` 二级预测核开发”
- `E_ret` 是 Premium 的第一核心核
- `E_ret_plus` 是 Premium 的最终核心核
- `X` 的本质是围绕 `E_ret` 的二级增强学习层
- 当前最合理的技术路线是：学习 `E_ret` 的条件残差修正
- `E_ret_plus = E_ret + delta_hat` 是当前第一版最专业、最现实的主线定义
- `P_fill / Cost / RiskPenalty / EV` 在 Premium 中的重要性仍然存在，但必须降位为增强器与解释器
- `P10 / P50 / P90` 不再作为主线起点，而应作为 `E_ret_plus` 稳定后的下游表达层
- Premium 的主线价值不在“更好看”，而在“更强、更准、更可闭环”
- 当前第一代码目标不是改展示，而是先落 `predict.py` 中的增强核

---

# 十八、下一步第一开发动作

下一次代码第一刀应明确为：

**在 `src/top10decision/premium/predict.py` 中落第一版 `X / EHX` 结构，形成最小可运行的 `E_ret_plus` 主链。**

在这一步中，至少应继续明确以下内容：

1. `delta_ret` 的真实标签定义  
2. `X` 当前第一版输入字段清单  
3. `eret_plus_value / eret_plus_delta / eret_plus_conf` 的输出契约  
4. 对应落盘字段如何进入 `premium_top30 / premium_full / premium_verify`  
5. 哪些旧版 Premium 输出字段应降级、延后或删除

---

# 十九、E_ret_plus 第一实现定义（V1）

本节开始把 Premium V3 从“方向契约”推进到“第一可实现契约”。

当前目标不是一次性做到最终最强形态，  
而是先锁定：

- 什么是第一版可运行的 `E_ret_plus`
- 什么是第一版真实可落地的 `X / EHX`
- 什么是第一版训练与推理必须共同遵守的输入输出定义

---

## 19.1 第一版总目标

`E_ret_plus V1` 的目标不是重新发明一套完整收益预测系统，  
而是：

**站在既有 `E_ret` 之上，做第一层条件残差校准。**

因此，第一版锁定为：

- 不推翻 `E_ret`
- 不脱离 `E_ret`
- 不重做独立收益主模型
- 不先追求分布
- 不先追求复杂解释

第一版只做一件事：

**让原始 `E_ret` 在真实交易语境下更准一点。**

---

## 19.2 第一版核心公式

第一版主线公式锁定为：

`delta_ret = y_true_ret - e_ret_pred`

其中：

- `y_true_ret`：真实目标收益率
- `e_ret_pred`：原始 `E_ret` 预测值

则：

`eret_plus_value = e_ret_pred + delta_hat`

其中：

- `delta_hat`：`X / EHX` 预测出的条件残差修正值

这一定义为当前第一版最高优先级定义。

---

## 19.3 y_true_ret 的第一版标签定义

第一版 `y_true_ret` 必须与既有 `E_ret` 的业务口径保持严格一致，不能另起一套口径。

当前建议锁定：

### 第一版近似定义
`y_true_ret = target_close / exec_anchor_price - 1`

其中：

- `target_close`：目标时点收盘价，当前近似为 `T+2` 收盘价
- `exec_anchor_price`：执行锚定价格，当前需与既有 `E_ret` 定义保持一致

---

## 19.4 exec_anchor_price 的原则

这一项必须与原始 `E_ret` 标签体系一致。

若原始 `E_ret` 已在训练链中隐含采用某个执行价格口径，则 `E_ret_plus` 第一版必须完全继承该口径，不允许私自改口径。

当前允许的做法是：

1. **优先复用原始 `E_ret` 的标签定义口径**
2. 在 `Premium Contract` 中只做业务定义，不擅自改动既有训练真值逻辑
3. 若后续发现 `E_ret` 口径需要统一修订，应单独发起“收益标签口径修订”，而不是在 `E_ret_plus V1` 内部偷改

因此，当前锁定原则为：

**`delta_ret` 的定义必须严格依附于原始 `E_ret` 真值定义。**

---

## 19.5 X / EHX-V1 的输入字段分层清单

第一版不追求全量特征，而追求：

- 少而硬
- 能跑通
- 能闭环
- 能解释
- 不漂移

当前建议分四层输入。

---

### A层：第一核心核输入（必须有）

- `eret_pred` / `e_ret_pred`
- `close`（T日收盘锚点）
- `trade_date`
- `target_date`（若推理态已可确定）
- `candidate_rank`（若已有）

说明：

这层代表：

- 原始收益判断核
- 当前现实价格锚点
- 当前样本基础定位

其中最核心的是：

- `e_ret_pred`
- `close`

---

### B层：执行修正输入（第一版建议纳入）

- `p_fill_pred`
- `cost_total` 或等价总成本字段
- `risk_penalty_total` 或等价风险惩罚字段
- `ev` 或等价 EV 字段

说明：

这层不是为了平权并列，而是为了回答：

- 这个收益是否容易兑现
- 这个收益是否会被吃掉
- 这个收益是否应被打折
- 收益 / 成本 / 风险是否一致

---

### C层：位置与状态输入（第一版建议选少量强字段）

优先只选少量最强字段，避免第一版过度膨胀。

当前建议候选：

- `pct_chg_t`
- `amplitude_t`
- `close_pos_n`（close 在近N日区间中的位置）
- `is_near_short_high`
- `turnover_rate`
- `volume_ratio` 或等价量能字段
- `market_regime` / `market_state`（若已有）
- `theme_strength` / `board_heat`（若已有）

说明：

第一版不要求把所有 raw 都吃进去，  
只要求纳入少量能显著影响 `E_ret` 偏差的现实状态字段。

---

### D层：样本经验输入（第一版可选，第二版优先强化）

若当前工程上已方便接入，则可纳入：

- `sample_maturity`
- `eret_hist_bias_bucket`
- `eret_hist_abs_err_bucket`
- `similar_case_count`

若当前暂未具备完整产物，则第一版可先留接口，不强制一次做完。

原则：

**第一版先把残差校准器跑起来，经验层可先弱接入，不允许因为追求完美而卡死主线。**

---

## 19.6 X / EHX-V1 第一版不纳入的内容

以下内容当前明确不作为 `EHX-V1` 的第一优先输入：

- 大规模手工规则打分
- 大量展示层文本标签
- 复杂分位展开
- 多头并行复杂模型集成
- ChatGPT 解释层字段
- 纯为页面观感服务的字段
- 与 `E_ret` 主线弱相关但数量庞大的杂特征

理由：

第一版的目标是先构建一个稳定、清晰、可闭环的二级增强核，  
不是一次性堆出一个巨大而混乱的系统。

---

## 19.7 E_ret_plus V1 的输出契约

第一版必须至少输出以下字段：

### 核心输出
- `eret_pred_raw`：原始 `E_ret`
- `eret_plus_value`：增强后的 `E_ret_plus`
- `eret_plus_delta`：修正值
- `eret_plus_direction`：up / flat / down
- `eret_plus_conf`：增强可信度

---

## 19.8 eret_plus_direction 的定义

用于快速表达此次增强的方向。

建议第一版规则：

- `delta_hat > +eps` → `up`
- `delta_hat < -eps` → `down`
- 其余 → `flat`

其中 `eps` 为防止微小噪声导致方向误判的容忍阈值。

---

## 19.9 eret_plus_conf 的第一版定义

第一版不要求一上来就做到严格概率化置信度，  
但必须给出一个基础可信度表达。

当前建议三档即可：

- `high`
- `mid`
- `low`

其来源可先采用简单规则近似，例如基于：

- 样本成熟度
- 类似样本数量
- 历史残差稳定度
- 当前输入完整度
- 模型输出稳定性

后续第二版再升级为更精细的数值置信度。

---

## 19.10 第一版落盘字段要求

以下字段后续应进入至少一个 Premium 主产物：

- `eret_pred_raw`
- `eret_plus_value`
- `eret_plus_delta`
- `eret_plus_direction`
- `eret_plus_conf`

推荐进入：

- `premium_top30_YYYYMMDD.csv`
- `premium_full_YYYYMMDD.csv`

并在验证文件中保留后验对照：

- `eret_pred_raw`
- `eret_plus_value`
- `y_true_ret`
- `raw_abs_err`
- `plus_abs_err`
- `improve_flag`

---

## 19.11 improve_flag 的定义

用于直接验证 `E_ret_plus` 是否比原始 `E_ret` 更好。

建议定义：

- 若 `abs(y_true_ret - eret_plus_value) < abs(y_true_ret - eret_pred_raw)`  
  则 `improve_flag = 1`
- 否则 `improve_flag = 0`

后续可据此统计：

- 改进率
- 平均误差下降幅度
- 分组样本下的改进稳定性

这是 Premium 是否真正创造新增量价值的关键验证指标。

---

## 19.12 EHX-V1 的验收标准

第一版不是看页面，而是看以下三件事：

### 1）是否跑通
- 能稳定生成 `eret_plus_value`
- 能稳定落盘
- 不破坏既有 Premium 主流程

### 2）是否闭环
- 能在验证文件中回写 `y_true_ret`
- 能比较 raw / plus 的误差差异
- 能形成最基本的改进统计

### 3）是否真增值
至少要能回答：

- 有多少样本 `E_ret_plus` 比 `E_ret` 更接近真实值
- 在哪些类型样本上更容易改进
- 是否存在明显系统性高估/低估被修正

---

## 19.13 第一版明确不追求的目标

第一版不追求：

- 漂亮页面
- 花哨解释
- 完整分布
- 一步到位超复杂模型
- 一上来就极高提升幅度

第一版追求的是：

**小而硬的真实增量。**

也就是：

- 跑通
- 验真
- 闭环
- 稳定
- 可继续增强

---

## 19.14 第一版完成后的下一步

只有当以下条件满足，才进入第二版：

1. `EHX-V1` 已稳定落盘  
2. `improve_flag` 已能统计  
3. 已能初步确认某些样本分组中存在稳定改进  
4. 当前主链未出现明显漂移或解释冲突

满足后，再进入：

- `EHX-V2`：增强可信度细化
- 条件分组校准
- 经验层强化
- 后续的 `P10 / P50 / P90`

---

## 19.15 当前锁定结论

`E_ret_plus V1` 当前正式锁定为：

**基于原始 `E_ret` 的条件残差校准器。**

其第一版本质不是新主模型，  
而是：

**对 `E_ret` 做最小但真实有效的二级增强。**
