# top10-decision

本仓库是 **TOP10 决策层（top10-decision）**，负责在 a-top10 的源数据基础上，计算可读的决策产物，并为人类手工交易与后续自动化留出稳定接口。

---

# Premium（溢价预测）— 手工交易版 V1 需求规范（锁死版）

## 1. 定位与边界

Premium 是给**人类手动交易**使用的“第 3 日溢价机会雷达”。

* ✅ Premium 必须输出：

  1. **Top 30 溢价研究预测表（预测表）**
  2. **Top 30 上涨命中验证表（验证表，T+2 收盘后生成）**
  3. **全量展开表（full）**，用于防止漏机会
  4. 风险提示、置信度、数据质量提示（只提示，不硬过滤）

* ❌ Premium 不包含：

  * “能不能买到”的硬过滤（避免漏票）
  * 组合收益/仓位决策（由人类决定）

---

## 2. 时间轴（核心契约，默认 horizon=2）

设 `T` 为信号日（交易日，收盘后生成报告）：

* **预测窗口（锁死）**：`T → T+2`（按交易日历推进两次，不是自然日）
* **报告生成时点**：`T` 收盘后立即生成（当日已无法交易）
* **人类执行参考**：`T+1` 开盘查看 `T` 的报告，自主买入；`T+2` 自主卖出（盘中/收盘由人决定）
* **验证表生成时点**：仅在 `T+2` 收盘后生成

非交易日处理（锁死）：

* 若 `T+1/T+2` 遇到非交易日，按交易日历顺延；
* 若 `T+2` 行情数据未到：状态为 **pending**（不得报错卡死）。

---

## 3. 输入数据（候选=源表全量，锁死）

### 3.1 主输入（必须）

* `data/pred/pred_source_latest.csv`（a-top10 全量源表）
* Premium 默认候选范围 = **源表全量**（不得默认过滤）。

### 3.2 行情真值（必须，用于实际值/验证）

* `data/market/daily_YYYYMMDD.csv`（由 Market Truth Layer 保障生成/缓存）。

### 3.3 决策产物（可选，仅用于“字段合并/标签”，不得过滤）

* `outputs/decision/*.csv`
* 合并键（锁死）：`trade_date + ts_code + name`（主键 trade_date+ts_code，name 用于补全/校验）

---

## 4. 输出（Top30 + 全量展开）

Premium 输出两块内容：

* **Top 30（默认展示）**
* **全量展开（full，可下载/展开）**

推荐固定输出文件：

* `outputs/premium/premium_top30_{T}.csv`
* `outputs/premium/premium_full_{T}.csv`
* `outputs/premium/premium_latest.csv`（指向最新一次 Top30 综合排序）
* `docs/reports/premium_latest.md`（人类可读摘要 + 两张表）

---

## 5. 排序维度（至少三种，页面可切换）

Premium 必须同时提供三种排序维度（CSV 内保留字段，前端可切换）：

1. **概率优先**：`p_premium` 降序
2. **幅度优先**：`e_premium` 降序（单位：百分比）
3. **综合排序（默认）**：`score_ev` 降序（综合考虑收益、概率、置信度与风险惩罚）

---

## 6. 字段规范（锁死）

### 6.1 基础字段（必须）

* `trade_date`（信号日 T）
* `target_date`（目标日 T+2）
* `ts_code`
* `name`

### 6.2 Premium 预测字段（必须）

* `p_premium`：上涨概率（0~1）
* `e_premium`：上涨幅度预测（%），表示 **预测 close(T+2) 相对 close(T) 的涨跌幅%**
* `score_ev`：综合分值（默认排序列）

### 6.3 风险与质量（必须）

* `risk_flags`：多标签（`;` 分隔）
* `confidence`：0~100
* `data_quality`：0~100
* `warning`：一句话风险提示

### 6.4 decision 合并字段（可选但推荐，字段加前缀避免冲突）

* `dec_rank`
* `dec_weight`
* `dec_reason`
* `dec_can_buy`（若有）
* `dec_p_fill`（若有）

### 6.5 追溯（必须）

* `run_id`
* `commit_sha`
* `created_at_utc`
* `model_version`

---

## 7. 报告页面必须包含两张表（锁死）

### 7.1 预测表（T 收盘后生成）

* 标题：`{T} → {T+2}　TOP 30 溢价概率研究报告`
* 左侧基础字段：排序 / 预测日期 / 收盘日期 / 代码 / 名称
* 右侧必须包含 Premium 新增字段：上涨概率 / 上涨幅度 / 综合分值（并可追加风险/置信度/质量）
* 再拼接 decision 合并字段：决策排名/权重/可买提示/决策标签（只做提示，不过滤）

### 7.2 验证表（仅 T+2 收盘后生成，顺序必须与预测表一致）

* 标题：`{T} → {T+2}　TOP 30 溢价概率为正预测命中率：xx.xx%`
* 表头字段：排序 / 预测日期 / 收盘日期 / 代码 / 名称 / 预测值 / 实际值 / 上涨命中
* 实际值定义（锁死）：

  * `actual_ret = close(T+2)/close(T) - 1`
  * “实际值”展示为百分比（例如 `+9.21%`）
* 上涨命中定义（锁死）：

  * `actual_ret > 0` → `是`
  * 否则 → `否`
* 命中率：Top30 中 “是”的数量 / 30
* 验证表行顺序：**必须与预测表 Top30 行顺序完全一致**（不可重新排序）

---

## 8. 开发原则（锁死）

* 候选不硬过滤：任何“可买/不可买”只作为标签提示；
* 交易日历驱动：非交易日顺延，行情未到则 pending；
* 输出契约稳定：字段名与文件名不得随意更改（外部已对接）。
