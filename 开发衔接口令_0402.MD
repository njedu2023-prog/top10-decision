# 开发衔接口令_0402.MD

继续 top10-decision 主线开发，严格以“提升系统最高价值”为唯一锚定，不回退旧话题，不重做 workflow，不改上下游路径，不推倒重来。

---

## 一、当前阶段定位（0402 版）

当前主线**不是**：

- workflow 能不能跑
- learning 主链有没有打通
- latest 工件能不能生成
- Premium 页面怎么做

当前主线已经明确切到：

# 学习系统对 Decision 层真实价值的继续收口  
重点是：
1. **P_fill 是否在 Decision 层退化成高位常数项**
2. **E_ret 的真实输出是否正常**
3. **E_ret → EV 的价值映射是否合理**
4. **系统当前偏低的 EV，究竟是系统坏了，还是系统开始更真实地反映市场**

---

## 二、截至 2026-04-02 的当前状态小结

### 1. 工程链路层：已经不是主矛盾

当前已经确认：

- workflow / learning 主链已基本收口
- `pfill_meta.json` / `eret_meta.json` / `learning_acceptance_latest` 已能产出并验收
- `decision_report_YYYYMMDD.md`
- `eval_YYYYMMDD.json`
- `decision_candidates_YYYYMMDD.csv`

这些核心产物都已经能稳定落地。

当前已经不是“系统打不通”的问题。

---

### 2. P_fill 当前状态：工程通过，但决策区分度仍可疑

当前已确认：

- P_fill 训练链路正常
- 在线推理已接通
- `p_fill_pred_src = model:lgbm`
- 当前线上仍走 LGBM
- `pfill_meta.json` 显示：
  - LR 的 AUC 更高
  - 但 LGBM 的 logloss / brier 更好
- 所以从“概率质量”角度，P_fill 当前优先用 LGBM是有依据的

但是：

- `decision_report_20260331.md`
- `decision_report_20260401.md`
- `decision_candidates_20260330.csv`
- `decision_candidates_20260331.csv`

都已显示大量候选 `P_fill ≈ 0.98` 或极接近 0.98。

当前判断：

- **P_fill 工程上没问题**
- **P_fill 当前主要问题不是“选错 LR/LGBM”**
- **而是高位常数化 / 高位拥挤，导致区分度不足**

当前不要轻易把 P_fill 切回 LR。  
主问题仍是：

# P_fill 为什么在 Decision 层近似退化成高位常数项

---

### 3. E_ret 当前状态：训练链路正常，但“价值逻辑”尚未收口

当前已确认：

- `build_eret_truth.py`
- `build_eret_trainset.py`
- `train_eret.py`
- `eret_meta.json`
- `sample_maturity_latest.csv`

这条链路已经基本查清：

#### 已确认成立的事实
1. E_ret 的训练目标列是：
   - `realized_ret_t1_to_t2`
2. 真值链路确实以 **T+2 市场真值** 为目标出口
3. 样本成熟度 `ERET_READY=1` 的时间语义是自洽的
4. 所以：
   - **E_ret 的“学习训练对标市场真值”这条链路本身没有问题**

这一步当前可以视为已通过。

---

### 4. E_ret 模型选择实验：已做过一次，结论已更新

之前已确认：

- `eret_meta.json` 上，LR 的 valid 指标优于 LGBM
- 因此曾怀疑线上默认 LGBM 可能不优

已经执行过一次正式实验：

- 修改 `src/top10decision/engines/eret_engine.py`
- 将 E_ret 在线优先级改成 **LR > LGBM > rule**
- 最新产物已验证：
  - `eret_pred_src = model:lr`
  - `eret_model_kind = lr`
  - candidates / report / engine 三层一致

说明：

# E_ret 已经真实切换到 LR 成功

但是实验结果也同时表明：

- 切成 LR 后，Decision 层并没有自动变强
- 20260401 这轮的输出没有因为 LR 而显著改善
- 因此可以确认：

# “E_ret valid 更好” ≠ “Decision 层最终更有价值”

这一点非常关键。

---

### 5. 当前最重要的新判断：问题已不再只是模型选择

现在已经确认：

- 训练链路没问题
- 真值链路没问题
- 在线接线没问题
- 模型切换也真实生效了

所以当前最值得锁定的新主问题是：

# E_ret → EV 这一步的价值映射是否合理

原因：

当前 `run_v2.py` 里，EV 的核心公式就是：

`EV = p_fill_pred * e_ret_pred - cost_est - risk_penalty`

并且当前已确认：

- `run_v2.py` 没有对 E_ret 再做额外 normalize / shrink / rank 映射
- report 展示的 `E_ret` 也是直接展示 `e_ret_pred`
- 真正可能改变 E_ret 的地方在 `eret_engine.py`
- 真正消费 E_ret 的地方在 `run_v2.py`

因此当前系统最关键的新判断是：

- **E_ret 的训练口径当前是合理的**
- 但**E_ret 作为“价值项”被直接线性乘入 EV，这一步尚未被证明合理**

---

### 6. 当前新的高价值判断：系统不一定坏了，可能只是更真实了

现在换了一个视角后，当前一个非常重要的新判断成立：

之前偏高、偏厚、看起来更好看的 E_ret / EV  
不一定是“正常值”。

相反，当前这版：

- 链路更自洽
- 真值更明确
- LR 切换已生效
- 候选池与报告口径更一致

因此：

# 完全有可能原来那版 E_ret 不正常，现在这版才更接近系统真实输出

这意味着当前要从系统级承认：

- 当前 EV 变低，不一定表示系统坏了
- 也可能表示系统开始更真实地反映市场可提炼 alpha 的厚度

这一步是当前 0402 的重要思想升级。

---

## 三、当前主线的正式结论

### 当前已经收口的部分

#### A. 工程链路层：已基本收口
- 不再是主矛盾

#### B. E_ret 真值训练链路：已基本收口
- 用 T+2 市场真值训练 E_ret 这条链成立

#### C. E_ret 模型切换实验：已完成
- 已证实切 LR 能生效
- 但未证明其系统价值更优

---

### 当前尚未收口的部分

#### 1. P_fill 的高位常数化问题
问题不是“能不能跑”，而是：

- 为什么大量候选 `P_fill ≈ 0.98`
- 是否导致其在 EV 中退化成低区分度常数项

#### 2. E_ret 的价值消费逻辑问题
当前真正未收口的是：

- E_ret 训练正确
- 但它是否适合被当前 EV 公式**直接线性消费**

#### 3. 学习层验证与决策层价值的错位问题
当前已经出现：

- 学习层 valid 更优
- 决策层不一定更优

说明当前存在：

# 学习验证口径 与 决策价值口径 可能错位

---

## 四、后续提升方向（按优先级排序）

### 第一优先级：继续深挖 Decision，不切 Premium

当前不要优先做：

- Premium 主线
- 展示层美化
- Workflow 大改
- Node.js 24
- 手动旧日期 latest 覆盖规则
- 历史补跑治理

这些都不是当前第一优先级。

当前第一主线仍然是：

# 决策价值收口

---

### 第二优先级：优先审 E_ret → EV 映射逻辑

重点继续查：

- `scripts/run_v2.py`
- `src/top10decision/engines/eret_engine.py`

要回答的问题：

1. E_ret 是否应该继续以**原始连续收益值**直接进 EV
2. E_ret 是否更适合做：
   - 排序因子
   - 分档因子
   - 非线性映射后再入 EV
3. 训练目标与决策目标之间，是否需要新增一层“对齐验证”

这一步是当前最重要的高价值提升方向。

---

### 第三优先级：补“决策层真实价值验证”

当前后续不应只看：

- RMSE
- R²
- Corr
- Directional Acc

而应增加真正贴近系统最高价值的验证，例如：

- TopN EV 厚度
- 正 EV 数量
- EV > 3% 数量
- TopN 稳定性
- 不同模型组合下的 Decision 层质量比较

即：

# 训练指标 ≠ 决策价值  
后续必须补这层验证

---

### 第四优先级：P_fill 常数化根因继续定位

当前继续盯：

- `models/pfill_meta.json`
- `src/top10decision/engines/pfill_engine.py`
- `decision_candidates_20260330.csv`
- `decision_candidates_20260331.csv`

要继续确认：

1. 原始 `p_fill_pred_model` 是否本来就极度高位集中
2. `p_fill_pred_final` 是否被 clip / 上限裁剪进一步压平
3. 当前 P_fill 是否虽然工程正确，但决策价值不足

---

## 五、下次进入后建议执行顺序

### 第一步
继续围绕 **Decision 价值收口** 推进，不切 Premium。

### 第二步
优先深挖：

- `run_v2.py`
- `eret_engine.py`

重点确认：

- E_ret 是否适合线性消费
- 还是需要映射 / 分层 / 转换后再进入 EV

### 第三步
在 E_ret 价值映射未确认前，不要再继续争论：

- LR 还是 LGBM 谁更好
- 报告是不是看起来更漂亮
- EV 为什么不够高

因为当前主问题已经不是“模型类型”，而是“价值消费逻辑”。

### 第四步
并行保留 P_fill 常数化问题，但不抢第一主线。

---

## 六、当前一句话总结

当前 top10-decision 已从“learning 主链是否正常、模型是否接通”的阶段，推进到：

# 工程与真值链路已基本通过，当前最关键的问题不再是模型是否工作，而是：
# E_ret 作为连续收益预测值，是否适合按当前方式直接进入 EV 价值计算；
# 同时 P_fill 仍存在高位常数化、区分度不足的问题；
# 当前系统偏低的 EV 也不一定表示系统坏了，反而可能表示系统开始更真实地反映市场。

---

## 七、下次开新对话时可直接粘贴

继续 top10-decision 主线开发，不回退旧话题，不重做 workflow，不改上下游路径，不推倒重来。

当前阶段已经不是 learning 主链能不能跑，而是：

Decision 层真实价值继续收口。

当前状态锚点如下：

1. workflow / learning 主链已基本收口，pfill_meta.json / eret_meta.json / learning_acceptance_latest 已能产出并验收。
2. E_ret 真值训练链路已基本确认正确：当前确实是用 T+2 市场真值训练 `realized_ret_t1_to_t2`。
3. E_ret 已真实切换到 LR 成功，但切换后未证明 Decision 层价值更优。
4. 当前关键问题已不再只是模型选择，而是：
   - E_ret → EV 的价值映射是否合理
   - P_fill 是否在 Decision 层高位常数化
5. 当前低 EV 不一定表示系统坏了，也可能表示系统开始更真实地反映市场。

下次进入后，按以下顺序执行：

第一步：继续深挖 Decision，不切 Premium  
第二步：优先检查 `scripts/run_v2.py` 与 `src/top10decision/engines/eret_engine.py`  
第三步：重点判断 E_ret 是否应继续以原始连续收益值直接进入 EV，还是应做映射 / 分层 / 变换  
第四步：并行保留 P_fill 常数化问题，但不抢第一主线

当前不要优先处理：

- Premium 主线
- workflow 大改
- Node.js 24
- 手动旧日期 latest 覆盖规则
- 历史补跑治理
- 展示层美化
